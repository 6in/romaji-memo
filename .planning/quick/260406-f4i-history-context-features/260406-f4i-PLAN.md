---
phase: quick
plan: 260406-f4i
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/providers/prompts.rs
  - src-tauri/src/commands/convert.rs
  - src-tauri/src/db/conversions.rs
  - src-tauri/src/db/migrations.rs
  - src-tauri/src/commands/history.rs
  - src-tauri/src/lib.rs
  - src/components/TitleBar.tsx
  - src/lib/tauri.ts
autonomous: true
must_haves:
  truths:
    - "変換時に直近履歴がプロンプトに含まれ、文脈を考慮した変換が行われる"
    - "ストッパー挿入後は、それ以前の履歴がプロンプトに含まれない"
    - "TitleBarの「新しい会話」ボタンでストッパーが挿入される"
  artifacts:
    - path: "src-tauri/src/providers/prompts.rs"
      provides: "build_system_prompt が recent_history を受け取り、プロンプトに履歴セクションを追加"
    - path: "src-tauri/src/db/conversions.rs"
      provides: "get_recent_context 関数（ストッパー考慮）、insert_stopper 関数"
    - path: "src-tauri/src/commands/convert.rs"
      provides: "変換前に直近履歴を取得してプロンプトに渡す"
    - path: "src/components/TitleBar.tsx"
      provides: "新しい会話ボタン"
  key_links:
    - from: "src-tauri/src/commands/convert.rs"
      to: "src-tauri/src/db/conversions.rs::get_recent_context"
      via: "spawn_blocking で呼び出し"
    - from: "src-tauri/src/commands/convert.rs"
      to: "src-tauri/src/providers/prompts.rs::build_system_prompt"
      via: "recent_history 引数を渡す"
    - from: "src/components/TitleBar.tsx"
      to: "src-tauri/src/commands/history.rs::new_conversation"
      via: "invoke('new_conversation')"
---

<objective>
変換精度向上のため直近変換履歴をAIプロンプトに含める機能と、会話コンテキストをリセットするストッパー機能を実装する。

Purpose: 同音異義語の誤変換を減らし、トピック切替時に前の文脈が干渉しないようにする。
Output: 履歴コンテキスト付き変換 + ストッパー機能 + 「新しい会話」ボタン
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src-tauri/src/providers/prompts.rs
@src-tauri/src/commands/convert.rs
@src-tauri/src/db/conversions.rs
@src-tauri/src/db/migrations.rs
@src-tauri/src/commands/history.rs
@src-tauri/src/lib.rs
@src/components/TitleBar.tsx
@src/lib/tauri.ts

<interfaces>
<!-- 既存の型・関数シグネチャ -->

From src-tauri/src/db/conversions.rs:
```rust
pub struct ConversionRecord {
    pub id: i64,
    pub input: String,
    pub output: String,
    pub style_id: String,
    pub intent: Option<String>,
    pub typo: Option<String>,
    pub provider_id: String,
    pub model: String,
    pub pinned: bool,
    pub created_at: String,
}

pub fn insert_conversion(conn: &Connection, input: &str, output: &str, style_id: &str, intent: Option<&str>, typo: Option<&str>, provider_id: &str, model: &str) -> Result<i64, rusqlite::Error>;
pub fn get_history(conn: &Connection, limit: i64, offset: i64, style_filter: Option<&str>) -> Result<Vec<ConversionRecord>, rusqlite::Error>;
```

From src-tauri/src/providers/prompts.rs:
```rust
pub fn build_system_prompt(style_id: &str) -> String;
```

From src-tauri/src/commands/convert.rs:
```rust
pub struct ConvertResult { pub converted: String, pub intent: String, pub typo: String, pub history_id: i64 }
pub async fn convert(state: State<AppState>, input: String, style_id: String, provider_id: String) -> Result<ConvertResult, String>;
```

From src-tauri/src/state.rs (推定):
```rust
pub struct AppState { pub db: Arc<Mutex<Connection>>, pub providers: Arc<RwLock<HashMap<String, Arc<dyn ProviderAdapter>>>> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB層 — ストッパー対応マイグレーション + get_recent_context / insert_stopper 関数追加</name>
  <files>src-tauri/src/db/migrations.rs, src-tauri/src/db/conversions.rs</files>
  <action>
1. `migrations.rs` に `MIGRATION_003` を追加:
   ```sql
   ALTER TABLE conversions ADD COLUMN is_stopper INTEGER NOT NULL DEFAULT 0;
   ```
   ※ SQLite の ALTER TABLE ADD COLUMN は NOT NULL + DEFAULT なら既存行に影響なし。
   マイグレーション適用ロジック（`run_migrations` 等）が既にある場合はそこに `MIGRATION_003` を追加登録する。なければ `db/mod.rs` のマイグレーション実行箇所を確認して追加。

2. `conversions.rs` に以下の2関数を追加:

   ```rust
   /// ストッパーレコードを挿入する（新しい会話の区切り）
   pub fn insert_stopper(conn: &Connection) -> Result<i64, rusqlite::Error> {
       conn.execute(
           "INSERT INTO conversions (input, output, style_id, provider_id, model, is_stopper) VALUES ('', '', '', '', '', 1)",
           [],
       )?;
       Ok(conn.last_insert_rowid())
   }

   /// 直近のストッパー以降の変換履歴を最大 limit 件取得する。
   /// 各エントリの input/output は先頭50文字で切る。
   /// ストッパーレコード自体は結果に含まない。
   pub fn get_recent_context(conn: &Connection, limit: i64) -> Result<Vec<(String, String)>, rusqlite::Error> {
       let mut stmt = conn.prepare(
           "SELECT input, output FROM conversions
            WHERE is_stopper = 0
              AND id > COALESCE((SELECT MAX(id) FROM conversions WHERE is_stopper = 1), 0)
            ORDER BY id DESC
            LIMIT ?1"
       )?;
       let rows = stmt.query_map(rusqlite::params![limit], |row| {
           let input: String = row.get(0)?;
           let output: String = row.get(1)?;
           // 先頭50文字で切る
           let input_trimmed: String = input.chars().take(50).collect();
           let output_trimmed: String = output.chars().take(50).collect();
           Ok((input_trimmed, output_trimmed))
       })?;
       let mut results: Vec<(String, String)> = rows.collect::<Result<Vec<_>, _>>()?;
       results.reverse(); // 古い順にする（プロンプト表示用）
       Ok(results)
   }
   ```

3. `map_row` 関数を更新 — `is_stopper` カラムが増えたため `ConversionRecord` に `is_stopper: bool` フィールドを追加するか、SELECT 文で `is_stopper` を含めないクエリはそのままにする。既存 `get_history` / `search_history` の SELECT に `is_stopper = 0` 条件を追加して、ストッパーレコードが履歴一覧に表示されないようにする。
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MIGRATION_003 が定義されマイグレーション実行に組み込まれている
    - insert_stopper がストッパーレコードを挿入できる
    - get_recent_context がストッパー以降の履歴を最大10件、50文字制限で返す
    - 既存の get_history / search_history がストッパーレコードを除外する
    - cargo check が通る
  </done>
</task>

<task type="auto">
  <name>Task 2: プロンプト層 + 変換コマンド — 履歴コンテキストをプロンプトに注入</name>
  <files>src-tauri/src/providers/prompts.rs, src-tauri/src/commands/convert.rs</files>
  <action>
1. `prompts.rs` の `build_system_prompt` シグネチャを変更:
   ```rust
   pub fn build_system_prompt(style_id: &str, recent_history: &[(&str, &str)]) -> String
   ```
   - `recent_history` が空でない場合、プロンプト末尾（JSON出力形式の前）に以下セクションを追加:
     ```
     直近の変換文脈（参考にして一貫性を保ってください）：
     - 「{input}」→「{output}」
     - 「{input}」→「{output}」
     ...
     ```
   - `recent_history` が空なら従来通りのプロンプト（セクション追加なし）

2. 既存テスト `test_all_eight_styles_defined` と `test_unknown_style_fallback` を更新:
   - 引数に `&[]`（空スライス）を渡すように修正
   - 新テスト追加: `test_with_recent_history` — 履歴2件を渡してプロンプトに「直近の変換文脈」が含まれることを検証

3. `commands/convert.rs` を更新:
   - 変換実行前（ステップ2のプロンプトビルド前）に `get_recent_context(conn, 10)` を呼び出す
   - `spawn_blocking` で DB アクセスして結果を取得
   - 取得した `Vec<(String, String)>` を `&[(&str, &str)]` に変換して `build_system_prompt` に渡す:
     ```rust
     let history_refs: Vec<(&str, &str)> = recent_context.iter().map(|(i, o)| (i.as_str(), o.as_str())).collect();
     let system = crate::providers::prompts::build_system_prompt(&style_id, &history_refs);
     ```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo test --manifest-path src-tauri/Cargo.toml -- prompts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - build_system_prompt が履歴付きプロンプトを生成する
    - 履歴なしの場合は従来と同じプロンプトが出力される
    - convert コマンドが変換前に直近履歴10件を取得してプロンプトに注入する
    - 全テストが通る
  </done>
</task>

<task type="auto">
  <name>Task 3: new_conversation コマンド + TitleBar「新しい会話」ボタン</name>
  <files>src-tauri/src/commands/history.rs, src-tauri/src/lib.rs, src/lib/tauri.ts, src/components/TitleBar.tsx</files>
  <action>
1. `commands/history.rs` に `new_conversation` コマンドを追加:
   ```rust
   #[tauri::command]
   pub async fn new_conversation(
       state: tauri::State<'_, crate::state::AppState>,
   ) -> Result<(), String> {
       let db = state.db.clone();
       tokio::task::spawn_blocking(move || {
           let conn = db.blocking_lock();
           crate::db::conversions::insert_stopper(&conn)
       })
       .await
       .map_err(|e| e.to_string())?
       .map_err(|e| e.to_string())?;
       Ok(())
   }
   ```

2. `lib.rs` の `generate_handler!` マクロに `commands::history::new_conversation` を追加。

3. `src/lib/tauri.ts` に `newConversation` 関数を追加:
   ```typescript
   export function newConversation(): Promise<void> {
     return invoke('new_conversation');
   }
   ```

4. `src/components/TitleBar.tsx` を更新:
   - `import { MessageSquarePlus } from 'lucide-react'` を追加（または `RotateCcw` 等、適切なアイコン）
   - `import { newConversation } from '../lib/tauri'` を追加
   - SettingsDialog の左隣あたりに「新しい会話」ボタンを追加:
     ```tsx
     <button
       onClick={async () => {
         try { await newConversation(); } catch (err) { console.error('Failed to start new conversation:', err); }
       }}
       title="新しい会話（コンテキストリセット）"
       className="p-1 rounded hover:bg-accent transition-colors"
     >
       <MessageSquarePlus size={12} className="text-muted-foreground" />
     </button>
     ```
   - ボタンは常に表示。クリック時にストッパーを挿入するだけ（UIフィードバックは console.log で十分、toast は既存パターンがあればそれに合わせる）
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - new_conversation コマンドが Tauri に登録されストッパーを挿入する
    - TitleBar に「新しい会話」ボタンが表示される
    - ボタンクリックで invoke('new_conversation') が呼ばれる
    - cargo check + tsc --noEmit が通る
  </done>
</task>

</tasks>

<verification>
1. `cargo check` — Rust コンパイルエラーなし
2. `cargo test -- prompts` — プロンプト関連テスト全通過
3. `npx tsc --noEmit` — TypeScript 型エラーなし
4. 手動確認: `npx tauri dev` で起動し、数回変換後に「新しい会話」を押して、次の変換で前の文脈が参照されないことを確認
</verification>

<success_criteria>
- 変換実行時に直近10件（50文字制限）の履歴がプロンプトに含まれる
- 履歴0件の場合は従来通りのプロンプトで動作する
- 「新しい会話」ボタン押下後、次の変換では押下前の履歴が参照されない
- ストッパーレコードが履歴一覧に表示されない
- 全テスト通過、型チェック通過
</success_criteria>

<output>
完了後、`.planning/quick/260406-f4i-history-context-features/260406-f4i-SUMMARY.md` を作成
</output>
