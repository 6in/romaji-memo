---
phase: 01-core-conversion-loop
plan: 01
subsystem: infra
tags: [tauri2, react19, typescript, tailwindcss-v4, shadcn-ui, zustand5, tanstack-query5, rusqlite, keyring, reqwest, tokio]

# Dependency graph
requires: []
provides:
  - Tauri 2 project scaffold compiling on macOS
  - Rust dependencies: reqwest 0.13 (rustls), rusqlite 0.39 (bundled), keyring 3.6, tokio 1, async-trait 0.1, thiserror 2
  - Frontend dependencies: Tailwind v4, shadcn/ui, Zustand 5, TanStack Query 5, lucide-react, sonner
  - Window configured as 420x600 transparent undecorated floating utility
  - Dark-green oklch theme with [data-theme=light] support
  - clipboard-manager plugin registered
affects: [02, 03, 04, 05, 06, 07, 08]

# Tech tracking
tech-stack:
  added:
    - tauri 2 (core framework)
    - tauri-plugin-clipboard-manager 2
    - reqwest 0.13 (json + rustls, no native-tls)
    - rusqlite 0.39 (bundled SQLite + FTS5)
    - keyring 3.6 (OS Keychain)
    - tokio 1 (full features)
    - async-trait 0.1
    - thiserror 2
    - tailwindcss 4 (via @tailwindcss/vite)
    - shadcn/ui (Tailwind v4 branch)
    - zustand 5
    - @tanstack/react-query 5
    - lucide-react
    - sonner
  patterns:
    - "Rust deps: no tauri-plugin-global-shortcut (Phase 2), no stream feature on reqwest (Phase 1)"
    - "Tailwind v4: @import tailwindcss in CSS, no tailwind.config.js, @theme inline for CSS vars"
    - "shadcn/ui uses CSS variable system wired to Tailwind v4 @theme"
    - "reqwest 0.13 uses rustls feature (not rustls-tls as in 0.12)"
    - "capabilities/default.json: clipboard-manager:default replaces opener:default"

key-files:
  created:
    - src-tauri/Cargo.lock
  modified:
    - src-tauri/Cargo.toml (all Phase 1 Rust deps at CLAUDE.md versions)
    - src-tauri/tauri.conf.json (420x600, decorations:false, transparent:true, alwaysOnTop)
    - src-tauri/src/lib.rs (clipboard plugin registered, greet command removed)
    - src-tauri/capabilities/default.json (opener:default -> clipboard-manager:default)
    - src/index.css (dark-green oklch theme + [data-theme=light])
    - src/App.tsx (QueryClientProvider shell)
    - src/main.tsx (import index.css)

key-decisions:
  - "reqwest 0.13 feature is rustls (not rustls-tls) — feature was renamed in 0.13"
  - "capabilities/default.json must be updated when removing tauri-plugin-opener to avoid build failure"
  - "shadcn/ui dark-green theme: override :root CSS vars to oklch 160-hue green values; keep shadcn @theme inline structure intact"
  - "Use [data-theme=light] for light mode toggle (not .light class) per design doc D-15"

patterns-established:
  - "Pattern 1: All Rust I/O must be async fn or spawn_blocking — no sync blocking in tauri commands"
  - "Pattern 2: API keys go to keyring crate only — never localStorage or config files"
  - "Pattern 3: AI streaming uses Tauri Channel API, not events"

requirements-completed: [PLAT-01, PLAT-02, PLAT-03, PLAT-04]

# Metrics
duration: 6min
completed: 2026-04-05
---

# Phase 1 Plan 01: Project Scaffold Summary

**Tauri 2 + React 19 project compiled with rusqlite 0.39, keyring 3.6, reqwest 0.13 (rustls), Tailwind v4 dark-green theme, and 420x600 transparent undecorated window**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-04T17:21:08Z
- **Completed:** 2026-04-04T17:27:52Z
- **Tasks:** 1
- **Files modified:** 7 modified, 1 created

## Accomplishments

- All Phase 1 Rust crates added at CLAUDE.md-specified versions (`cargo check` passes)
- Window configured as 420x600 transparent undecorated always-on-top floating utility
- Dark-green oklch theme applied with light mode support via `[data-theme="light"]`
- shadcn/ui, Zustand 5, TanStack Query 5, lucide-react, sonner all installed and importable
- `npm run build` exits 0 (Vite + TypeScript build clean)

## Task Commits

1. **Task 1: Scaffold Tauri 2 project and install all dependencies** - `f590736` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `src-tauri/Cargo.toml` - All Phase 1 Rust deps at CLAUDE.md versions; removed tauri-plugin-opener
- `src-tauri/tauri.conf.json` - Window 420x600, decorations:false, transparent:true, alwaysOnTop:true, clipboard-manager plugin
- `src-tauri/src/lib.rs` - clipboard-manager plugin registered; greet command removed
- `src-tauri/capabilities/default.json` - opener:default replaced with clipboard-manager:default
- `src/index.css` - Dark-green oklch theme with @theme inline, :root vars, [data-theme="light"] block
- `src/App.tsx` - Minimal QueryClientProvider shell
- `src/main.tsx` - Added import of index.css
- `src-tauri/Cargo.lock` - Generated by cargo check

## Decisions Made

- reqwest 0.13 renamed the TLS feature from `rustls-tls` to `rustls` — updated Cargo.toml accordingly with `default-features = false`
- capabilities/default.json must reference plugins that are actually in Cargo.toml — removed `opener:default`, added `clipboard-manager:default`
- shadcn/ui's CSS variable structure is preserved and dark-green oklch values are applied to `:root` (dark default) and `[data-theme="light"]`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed reqwest 0.13 rustls feature name**
- **Found during:** Task 1 (cargo check)
- **Issue:** Plan specified `features = ["json", "rustls-tls"]` but reqwest 0.13 renamed the feature to `rustls`; `cargo check` failed with "package does not have that feature"
- **Fix:** Changed to `features = ["json", "rustls"], default-features = false` in Cargo.toml
- **Files modified:** src-tauri/Cargo.toml
- **Verification:** cargo check exits 0
- **Committed in:** f590736

**2. [Rule 3 - Blocking] Fixed capabilities referencing removed plugin**
- **Found during:** Task 1 (cargo check after Cargo.toml update)
- **Issue:** `capabilities/default.json` referenced `opener:default` permission from `tauri-plugin-opener` which was removed from Cargo.toml; build script failed with "Permission opener:default not found"
- **Fix:** Replaced `opener:default` with `clipboard-manager:default` in capabilities file
- **Files modified:** src-tauri/capabilities/default.json
- **Verification:** cargo check exits 0
- **Committed in:** f590736

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both fixes were required for correct compilation. No scope creep. The reqwest feature rename is a known breaking change in 0.13.

## Issues Encountered

- `npm create tauri-app` requires a TTY for interactive prompts — used `--yes` flag to bypass. The main repo already had the scaffold committed (initial commit), so this plan's work was entirely the configuration layer on top.
- `npx shadcn@latest init --defaults` required `src/index.css` with `@import "tailwindcss"` to exist before running; created it first then re-ran init.

## User Setup Required

None — no external service configuration required at this stage. API key setup is Plan 03.

## Next Phase Readiness

- Plan 02 (SQLite schema) can proceed immediately — rusqlite 0.39 with bundled feature is in Cargo.toml
- Plan 03 (keyring/settings) can proceed — keyring 3.6 is in Cargo.toml
- Plan 04 (AI providers) can proceed — reqwest 0.13 with rustls is in Cargo.toml
- All frontend plans (05-08) can proceed — Tailwind v4, shadcn/ui, Zustand, TanStack Query are installed

---
*Phase: 01-core-conversion-loop*
*Completed: 2026-04-05*
