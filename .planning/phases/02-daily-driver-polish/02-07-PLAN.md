---
phase: 02-daily-driver-polish
plan: 07
type: execute
wave: 4
depends_on: [01, 05]
files_modified:
  - src-tauri/src/providers/copilot.rs
  - src-tauri/src/providers/mod.rs
  - src-tauri/src/lib.rs
  - src/components/settings/ProviderSettings.tsx
autonomous: false
requirements:
  - PROV-03

must_haves:
  truths:
    - "Copilot を選択した場合、変換が実行できるか明確なエラーメッセージが表示される"
    - "プロバイダー設定で Copilot を追加すると「GitHub Copilot (実験的)」ラベルが表示される"
    - "Device Flow で取得した token が OS Keychain に保存される"
    - "Copilot 経由で変換が実行できる (または明確なエラーメッセージ)"
  artifacts:
    - path: "src-tauri/src/providers/copilot.rs"
      provides: "CopilotAdapter — Device Flow OAuth + Chat Completions"
      min_lines: 100
  key_links:
    - from: "src-tauri/src/providers/copilot.rs"
      to: "src-tauri/src/providers/mod.rs"
      via: "ProviderAdapter trait implementation"
      pattern: "impl ProviderAdapter for CopilotAdapter"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/providers/copilot.rs"
      via: "adapter instantiation in setup"
      pattern: "copilot"
    - from: "src/components/settings/ProviderSettings.tsx"
      to: "src/lib/tauri.ts"
      via: "startCopilotAuth/pollCopilotAuth invoke wrappers"
      pattern: "startCopilotAuth|pollCopilotAuth"
---

<objective>
GitHub Copilot Device Flow OAuth adapter を実験的機能として実装する。

Purpose: Copilot ライセンスを持つユーザーが追加の API キー不要で変換を利用できるようにする。ToS リスクがあるため実験的ラベル付き。
Output: CopilotAdapter (Rust)、lib.rs でのアダプター登録、ProviderSettings での警告表示。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-daily-driver-polish/02-RESEARCH.md
@.planning/phases/02-daily-driver-polish/02-01-SUMMARY.md
@.planning/phases/02-daily-driver-polish/02-05-SUMMARY.md

<interfaces>
From src-tauri/src/providers/mod.rs:
```rust
#[async_trait::async_trait]
pub trait ProviderAdapter: Send + Sync {
    async fn complete(&self, req: CompletionRequest) -> Result<CompletionResponse, ProviderError>;
    fn name(&self) -> &str;
    fn provider_type(&self) -> &str;
    fn model_id(&self) -> &str;
}

pub struct ProviderConfig {
    pub id: String, pub name: String, pub adapter: String,
    pub base_url: Option<String>, pub api_key: Option<String>,
    pub model: String, pub enabled: bool,
}
```

From src-tauri/src/keychain.rs:
```rust
pub fn get_api_key(keychain_lock: &Mutex<()>, provider_id: &str) -> Result<Option<String>, String>;
pub fn set_api_key(keychain_lock: &Mutex<()>, provider_id: &str, api_key: &str) -> Result<(), String>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CopilotAdapter — Device Flow OAuth + Chat Completions</name>
  <files>
    src-tauri/src/providers/copilot.rs,
    src-tauri/src/providers/mod.rs,
    src-tauri/src/lib.rs
  </files>
  <read_first>
    src-tauri/src/providers/mod.rs,
    src-tauri/src/providers/openai.rs,
    src-tauri/src/lib.rs,
    src-tauri/src/keychain.rs,
    src-tauri/src/state.rs,
    .planning/phases/02-daily-driver-polish/02-RESEARCH.md
  </read_first>
  <action>
**1. 新規: src-tauri/src/providers/copilot.rs**

RESEARCH.md Pattern 5 に基づく実装:

```rust
use crate::providers::{
    CompletionRequest, CompletionResponse, ProviderAdapter, ProviderError, TokenUsage,
};
use serde::{Deserialize, Serialize};

const GITHUB_CLIENT_ID: &str = "Iv1.b507a08c87ecfe98"; // VS Code Copilot client ID
const DEVICE_CODE_URL: &str = "https://github.com/login/device/code";
const TOKEN_URL: &str = "https://github.com/login/oauth/access_token";
const COPILOT_TOKEN_URL: &str = "https://api.github.com/copilot_internal/v2/token";
const COPILOT_CHAT_URL: &str = "https://api.githubcopilot.com/chat/completions";

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeviceCodeResponse {
    pub device_code: String,
    pub user_code: String,
    pub verification_uri: String,
    pub expires_in: u64,
    pub interval: u64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
struct OAuthTokenResponse {
    access_token: Option<String>,
    error: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
struct CopilotTokenResponse {
    token: String,
    expires_at: u64,
}

pub struct CopilotAdapter {
    oauth_token: String,
    model: String,
    client: reqwest::Client,
}

impl CopilotAdapter {
    pub fn new(oauth_token: String, model: String) -> Self {
        let client = reqwest::Client::builder()
            .timeout(std::time::Duration::from_secs(30))
            .build()
            .expect("Failed to build reqwest client");
        Self { oauth_token, model, client }
    }

    async fn get_copilot_token(&self) -> Result<String, ProviderError> {
        let resp = self.client
            .get(COPILOT_TOKEN_URL)
            .header("Authorization", format!("token {}", self.oauth_token))
            .header("User-Agent", "romaji-memo/0.1.0")
            .send()
            .await?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(ProviderError::Api { status, body });
        }
        let token_resp: CopilotTokenResponse = resp.json().await
            .map_err(|e| ProviderError::Parse(format!("Copilot token parse error: {}", e)))?;
        Ok(token_resp.token)
    }
}

#[async_trait::async_trait]
impl ProviderAdapter for CopilotAdapter {
    async fn complete(&self, req: CompletionRequest) -> Result<CompletionResponse, ProviderError> {
        let copilot_token = self.get_copilot_token().await?;
        let body = serde_json::json!({
            "model": self.model,
            "messages": [
                { "role": "system", "content": req.system },
                { "role": "user", "content": req.user_message },
            ],
            "max_tokens": req.max_tokens,
            "temperature": 0.3,
        });
        let resp = self.client
            .post(COPILOT_CHAT_URL)
            .header("Authorization", format!("Bearer {}", copilot_token))
            .header("Content-Type", "application/json")
            .header("User-Agent", "romaji-memo/0.1.0")
            .header("Editor-Version", "vscode/1.90.0")
            .header("Copilot-Integration-Id", "vscode-chat")
            .json(&body)
            .send()
            .await?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(ProviderError::Api { status, body });
        }
        let json: serde_json::Value = resp.json().await
            .map_err(|e| ProviderError::Parse(e.to_string()))?;
        let content = json["choices"][0]["message"]["content"]
            .as_str().unwrap_or("").to_string();
        let model = json["model"].as_str().unwrap_or(&self.model).to_string();
        Ok(CompletionResponse {
            content, model,
            usage: Some(TokenUsage {
                input_tokens: json["usage"]["prompt_tokens"].as_u64().map(|v| v as u32),
                output_tokens: json["usage"]["completion_tokens"].as_u64().map(|v| v as u32),
            }),
        })
    }
    fn name(&self) -> &str { "GitHub Copilot" }
    fn provider_type(&self) -> &str { "copilot" }
    fn model_id(&self) -> &str { &self.model }
}

pub async fn start_device_flow() -> Result<DeviceCodeResponse, ProviderError> {
    let client = reqwest::Client::new();
    let resp = client.post(DEVICE_CODE_URL)
        .header("Accept", "application/json")
        .form(&[("client_id", GITHUB_CLIENT_ID), ("scope", "read:user")])
        .send().await?;
    if !resp.status().is_success() {
        let status = resp.status().as_u16();
        let body = resp.text().await.unwrap_or_default();
        return Err(ProviderError::Api { status, body });
    }
    resp.json().await.map_err(|e| ProviderError::Parse(e.to_string()))
}

pub async fn poll_for_token(device_code: &str, interval: u64) -> Result<String, ProviderError> {
    let client = reqwest::Client::new();
    loop {
        tokio::time::sleep(std::time::Duration::from_secs(interval)).await;
        let resp = client.post(TOKEN_URL)
            .header("Accept", "application/json")
            .form(&[
                ("client_id", GITHUB_CLIENT_ID),
                ("device_code", device_code),
                ("grant_type", "urn:ietf:params:oauth:grant-type:device_code"),
            ]).send().await?;
        let token_resp: OAuthTokenResponse = resp.json().await
            .map_err(|e| ProviderError::Parse(e.to_string()))?;
        if let Some(token) = token_resp.access_token { return Ok(token); }
        match token_resp.error.as_deref() {
            Some("authorization_pending") => continue,
            Some("slow_down") => { tokio::time::sleep(std::time::Duration::from_secs(5)).await; continue; }
            Some(e) => return Err(ProviderError::Parse(format!("OAuth error: {}", e))),
            None => continue,
        }
    }
}
```

**2. providers/mod.rs に追加:** `pub mod copilot;`

**3. lib.rs の setup() 内のアダプター初期化ループに copilot ケース追加:**

既存の `match p.adapter.as_str()` 内に:
```rust
"copilot" => {
    let key = match api_key {
        Some(k) => k,
        None => { eprintln!("[romaji-memo] Warning: No OAuth token for Copilot '{}'. Use settings UI.", p.id); continue; }
    };
    Arc::new(providers::copilot::CopilotAdapter::new(key, p.model.clone()))
}
```

**4. Copilot Device Flow の Tauri コマンド追加 (commands/providers.rs):**

```rust
#[tauri::command]
pub async fn start_copilot_auth() -> Result<crate::providers::copilot::DeviceCodeResponse, String> {
    crate::providers::copilot::start_device_flow().await.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn poll_copilot_auth(
    state: tauri::State<'_, crate::state::AppState>,
    device_code: String, interval: u64, provider_id: String,
) -> Result<(), String> {
    let token = crate::providers::copilot::poll_for_token(&device_code, interval).await.map_err(|e| e.to_string())?;
    crate::keychain::set_api_key(&state.keychain_lock, &provider_id, &token)?;
    Ok(())
}
```

lib.rs の `invoke_handler` に追加: `commands::providers::start_copilot_auth, commands::providers::poll_copilot_auth,`
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/providers/copilot.rs exists
    - copilot.rs contains `impl ProviderAdapter for CopilotAdapter`
    - copilot.rs contains `pub async fn start_device_flow(`
    - copilot.rs contains `pub async fn poll_for_token(`
    - copilot.rs contains `COPILOT_CHAT_URL`
    - src-tauri/src/providers/mod.rs contains `pub mod copilot`
    - src-tauri/src/lib.rs contains `"copilot"` match arm
    - src-tauri/src/commands/providers.rs contains `pub async fn start_copilot_auth(`
    - src-tauri/src/commands/providers.rs contains `pub async fn poll_copilot_auth(`
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
  <done>CopilotAdapter が ProviderAdapter trait を実装し、Device Flow OAuth + Chat Completions に対応。Tauri コマンド登録済み</done>
</task>

<task type="auto">
  <name>Task 2: ProviderSettings に Copilot 認証 UI + 実験的ラベル</name>
  <files>src/components/settings/ProviderSettings.tsx, src/lib/tauri.ts</files>
  <read_first>
    src/components/settings/ProviderSettings.tsx,
    src/lib/tauri.ts,
    .planning/phases/02-daily-driver-polish/02-UI-SPEC.md
  </read_first>
  <action>
**1. tauri.ts に Copilot コマンド invoke ラッパー追加:**

```typescript
export interface DeviceCodeResponse {
  deviceCode: string;
  userCode: string;
  verificationUri: string;
  expiresIn: number;
  interval: number;
}

export async function startCopilotAuth(): Promise<DeviceCodeResponse> {
  return invoke<DeviceCodeResponse>('start_copilot_auth');
}

export async function pollCopilotAuth(
  deviceCode: string, interval: number, providerId: string,
): Promise<void> {
  return invoke('poll_copilot_auth', { deviceCode, interval, providerId });
}
```

**2. ProviderSettings.tsx に Copilot 認証フロー追加:**

import: `import { startCopilotAuth, pollCopilotAuth } from '../../lib/tauri';`

状態追加:
```typescript
const [copilotAuthPending, setCopilotAuthPending] = useState(false);
const [copilotUserCode, setCopilotUserCode] = useState('');
const [copilotVerificationUri, setCopilotVerificationUri] = useState('');
```

adapter が "copilot" の場合、API key 入力の代わりに Device Flow UI を表示:
- 「GitHub Copilot (実験的) -- Device Flow で認証します。」説明文
- 「注意: GitHub Copilot の内部 API を使用します。GitHub の利用規約に抵触する可能性があります。自己責任でご利用ください。」警告文 (text-[10px] text-destructive)
- 「GitHub で認証」ボタン → startCopilotAuth → userCode 表示 → pollCopilotAuth → toast 成功
- adapter select に `<option value="copilot">Copilot (実験的)</option>` 追加
- プロバイダー名に copilot 時 `(実験的)` サフィックス表示
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/tauri.ts contains `export interface DeviceCodeResponse`
    - src/lib/tauri.ts contains `export async function startCopilotAuth(`
    - src/lib/tauri.ts contains `export async function pollCopilotAuth(`
    - ProviderSettings.tsx contains `startCopilotAuth` import
    - ProviderSettings.tsx contains `自己責任でご利用ください` warning
    - ProviderSettings.tsx contains `GitHub で認証` button
    - ProviderSettings.tsx contains `(実験的)` suffix
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Copilot Device Flow 認証 UI + 実験的ラベル + 警告文がすべて実装済み</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 2 全機能のエンドツーエンド検証</name>
  <files>N/A</files>
  <action>Phase 2 で実装した全機能を手動で検証する。npx tauri dev でアプリを起動し、以下のシナリオを実行する。</action>
  <what-built>Phase 2 全機能: グローバルホットキー、履歴検索/ピン/削除、ドラフトバッファ、設定ダイアログ (プロバイダー/スタイル/全般)、カスタムスタイル、Copilot (実験的)</what-built>
  <how-to-verify>
1. `npx tauri dev` でアプリを起動
2. グローバルホットキー (WINX-02): Cmd+Shift+R でウィンドウ非表示/再表示
3. ドラフトバッファ (BUFF-01-04): 変換 → バッファに追加 → ドラッグ並替 → 全コピー
4. 履歴検索 (HIST-04): 検索バーにキーワード → 結果フィルタ
5. スタイルフィルター (HIST-05): チップ選択 → 該当履歴のみ
6. ピン (HIST-06): ピンアイコン → 先頭移動
7. 履歴削除 (HIST-07): ホバー → ゴミ箱 → 即削除
8. 設定ダイアログ: ギアアイコン → 3タブ確認
9. プロバイダー設定 (PROV-05/06): 編集 → 接続テスト → バッジ確認
10. カスタムスタイル (CONV-07): 追加 → StyleSelector に表示
11. 全般設定 (HIST-08): 件数制限変更 → 保存
  </how-to-verify>
  <verify>npx tauri dev が起動し、上記全シナリオが動作すること</verify>
  <done>全 Phase 2 機能が実機で動作確認済み</done>
  <resume-signal>"approved" と入力するか、問題点を記述してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> GitHub OAuth | Device Flow で OAuth token を取得 |
| App -> copilot_internal API | セッション token を取得 |
| App -> api.githubcopilot.com | Chat Completions API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Spoofing | Device Flow client_id | accept | VS Code の公開 client_id を使用。ToS リスクは UI で明示的に警告 |
| T-02-18 | Information Disclosure | OAuth token in Keychain | mitigate | token は OS Keychain に保存。providers.json には `"<encrypted>"` のみ。フロントエンドに token を返さない |
| T-02-19 | Repudiation | Copilot ToS violation | mitigate | UI に「実験的」ラベルと「自己責任」警告を明示。ユーザーが能動的に認証を開始する必要がある |
| T-02-20 | Tampering | copilot_internal endpoint change | accept | 内部 API は予告なく変更される可能性がある。エラー時は明確なメッセージを表示 |
</threat_model>

<verification>
- `cargo build` 成功
- `npx tsc --noEmit` 成功
- Copilot adapter が ProviderAdapter を実装
- Device Flow UI が ProviderSettings に表示
- Human verification で全機能動作確認
</verification>

<success_criteria>
- cargo build + tsc 成功
- CopilotAdapter が complete/name/provider_type/model_id を実装
- Device Flow のコマンド (start_copilot_auth, poll_copilot_auth) が登録済み
- ProviderSettings に Copilot 認証 UI + 実験的ラベル + 警告文
- Human verification パス
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-driver-polish/02-07-SUMMARY.md`
</output>
