---
phase: 02-daily-driver-polish
plan: 06
type: execute
wave: 3
depends_on: [02, 05]
files_modified:
  - src/components/settings/SettingsDialog.tsx
  - src/components/settings/StyleManager.tsx
  - src/components/settings/HistorySettings.tsx
  - src/components/StyleSelector.tsx
  - src/lib/styles.ts
autonomous: true
requirements:
  - CONV-07
  - HIST-08

must_haves:
  truths:
    - "「スタイル」タブでカスタムスタイルの一覧・追加・編集・削除ができる"
    - "カスタムスタイルが StyleSelector にビルトインと並んで表示される"
    - "スタイル削除時にインライン確認「本当に削除しますか？」が表示される"
    - "「全般」タブで履歴件数制限を設定できる (デフォルト 1000)"
  artifacts:
    - path: "src/components/settings/StyleManager.tsx"
      provides: "カスタムスタイル CRUD UI"
      min_lines: 100
    - path: "src/components/settings/HistorySettings.tsx"
      provides: "履歴件数設定 UI"
      min_lines: 30
  key_links:
    - from: "src/components/settings/StyleManager.tsx"
      to: "src/hooks/useCustomStyles.ts"
      via: "useCustomStyles/useCreateStyle/useUpdateStyle/useDeleteStyle"
      pattern: "useCustomStyles|useCreateStyle"
    - from: "src/components/StyleSelector.tsx"
      to: "src/hooks/useCustomStyles.ts"
      via: "useCustomStyles() for merged style list"
      pattern: "useCustomStyles"
---

<objective>
設定ダイアログの「スタイル」と「全般」タブを実装し、StyleSelector にカスタムスタイルを統合する。

Purpose: ユーザーが独自のスタイル (独自プロンプト) を作成し、変換に使えるようにする。履歴件数制限でストレージを管理できるようにする。
Output: StyleManager.tsx, HistorySettings.tsx (新規)、SettingsDialog/StyleSelector 更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-daily-driver-polish/02-UI-SPEC.md
@.planning/phases/02-daily-driver-polish/02-02-SUMMARY.md
@.planning/phases/02-daily-driver-polish/02-05-SUMMARY.md

<interfaces>
From src/hooks/useCustomStyles.ts (Plan 02):
```typescript
export function useCustomStyles(): UseQueryResult<CustomStyle[]>;
export function useCreateStyle(): UseMutationResult<void, Error, { id, label, emoji, prompt, sortOrder }>;
export function useUpdateStyle(): UseMutationResult<void, Error, { id, label, emoji, prompt }>;
export function useDeleteStyle(): UseMutationResult<void, Error, string>;
```

From src/lib/tauri.ts (Plan 02):
```typescript
export interface CustomStyle { id: string; label: string; emoji: string; prompt: string; sortOrder: number; createdAt: string; }
export async function getHistoryLimit(): Promise<number>;
export async function setHistoryLimit(limit: number): Promise<void>;
```

From src/lib/styles.ts:
```typescript
export interface StylePreset { id: string; label: string; emoji: string; }
export const BUILT_IN_STYLES: StylePreset[];
```

From src/components/settings/SettingsDialog.tsx (Plan 05):
```typescript
// TabsContent value="styles" にプレースホルダーあり → StyleManager に置換
// TabsContent value="general" にプレースホルダーあり → HistorySettings に置換
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StyleManager コンポーネント + SettingsDialog 統合</name>
  <files>
    src/components/settings/StyleManager.tsx,
    src/components/settings/SettingsDialog.tsx
  </files>
  <read_first>
    src/components/settings/SettingsDialog.tsx,
    src/hooks/useCustomStyles.ts,
    src/lib/tauri.ts,
    src/lib/styles.ts,
    .planning/phases/02-daily-driver-polish/02-UI-SPEC.md
  </read_first>
  <action>
**1. 新規: src/components/settings/StyleManager.tsx**

UI-SPEC の StyleManager 仕様に準拠:

```typescript
import { useState } from 'react';
import { Plus, Trash2 } from 'lucide-react';
import { Input } from '../ui/input';
import { useCustomStyles, useCreateStyle, useUpdateStyle, useDeleteStyle } from '../../hooks/useCustomStyles';
```

**状態:**
```typescript
const [editingId, setEditingId] = useState<string | null>(null);
const [isAdding, setIsAdding] = useState(false);
const [confirmDeleteId, setConfirmDeleteId] = useState<string | null>(null);
const [form, setForm] = useState({ label: '', emoji: '', prompt: '' });
```

**カスタムスタイルリスト:**
- `const { data: styles, isLoading } = useCustomStyles();`
- 各スタイルをカード行で表示 (ProviderSettings と同パターン)
- カード: `<div className="bg-card border border-border rounded-md p-3 mb-2">`
- 非編集時: emoji + label 表示 + クリックで編集展開

**インライン編集フォーム:**
- Name: `<Input className="text-sm" value={form.label} onChange={...} placeholder="スタイル名" />`
- Emoji: `<Input className="text-sm w-16" value={form.emoji} onChange={...} placeholder="絵文字" maxLength={4} />`
- Prompt: `<textarea className="w-full text-xs font-mono border border-border rounded-md p-2 bg-muted resize-none" rows={4} value={form.prompt} onChange={...} placeholder="変換プロンプト..." />`
- Save: `useUpdateStyle().mutate({ id: editingId, label: form.label, emoji: form.emoji, prompt: form.prompt })` → 成功時 `setEditingId(null)`
- Cancel: `setEditingId(null)`

**削除 (UI-SPEC: インライン確認):**
```tsx
{confirmDeleteId === style.id ? (
  <div className="flex items-center gap-2 mt-2 text-xs">
    <span className="text-destructive">本当に削除しますか？</span>
    <button
      onClick={() => { deleteMutation.mutate(style.id); setConfirmDeleteId(null); }}
      className="text-destructive font-medium hover:underline"
    >削除</button>
    <button
      onClick={() => setConfirmDeleteId(null)}
      className="text-muted-foreground hover:underline"
    >キャンセル</button>
  </div>
) : (
  <button onClick={() => setConfirmDeleteId(style.id)} className="p-1">
    <Trash2 size={12} className="text-muted-foreground hover:text-destructive" />
  </button>
)}
```

**「スタイルを追加」ボタン:**
```tsx
<button
  onClick={() => {
    setIsAdding(true);
    setForm({ label: '', emoji: '✨', prompt: '' });
  }}
  className="bg-primary text-primary-foreground text-xs px-3 py-1.5 rounded-md flex items-center gap-1"
>
  <Plus size={12} /> スタイルを追加
</button>
```

追加保存時:
```typescript
createMutation.mutate({
  id: `custom-${Date.now()}`,
  label: form.label,
  emoji: form.emoji,
  prompt: form.prompt,
  sortOrder: (styles?.length ?? 0) + 1,
});
```

**2. SettingsDialog.tsx 更新:**

import 追加:
```typescript
import { StyleManager } from './StyleManager';
```

TabsContent value="styles" のプレースホルダーを置換:
```tsx
<TabsContent value="styles" className="flex-1 overflow-y-auto m-0 p-4">
  <StyleManager />
</TabsContent>
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/settings/StyleManager.tsx exists
    - StyleManager.tsx contains `useCustomStyles(` hook
    - StyleManager.tsx contains `useCreateStyle(` hook
    - StyleManager.tsx contains `useUpdateStyle(` hook
    - StyleManager.tsx contains `useDeleteStyle(` hook
    - StyleManager.tsx contains `スタイルを追加` button label
    - StyleManager.tsx contains `本当に削除しますか` inline confirmation
    - StyleManager.tsx contains `font-mono` on prompt textarea
    - StyleManager.tsx contains `rows={4}` on prompt textarea
    - src/components/settings/SettingsDialog.tsx contains `import { StyleManager }` 
    - src/components/settings/SettingsDialog.tsx contains `<StyleManager />`
    - src/components/settings/SettingsDialog.tsx does NOT contain `スタイル設定は準備中` (placeholder removed)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>StyleManager コンポーネント (CRUD + インライン確認削除) が完成し、SettingsDialog に統合済み</done>
</task>

<task type="auto">
  <name>Task 2: HistorySettings + StyleSelector カスタムスタイル統合</name>
  <files>
    src/components/settings/HistorySettings.tsx,
    src/components/settings/SettingsDialog.tsx,
    src/components/StyleSelector.tsx,
    src/lib/styles.ts
  </files>
  <read_first>
    src/components/settings/SettingsDialog.tsx,
    src/components/StyleSelector.tsx,
    src/lib/styles.ts,
    src/store/settingsStore.ts,
    src/hooks/useCustomStyles.ts,
    src/lib/tauri.ts,
    .planning/phases/02-daily-driver-polish/02-UI-SPEC.md
  </read_first>
  <action>
**1. 新規: src/components/settings/HistorySettings.tsx**

UI-SPEC: 履歴件数制限設定。

```typescript
import { useState, useEffect } from 'react';
import { Input } from '../ui/input';
import { getHistoryLimit, setHistoryLimit } from '../../lib/tauri';
import { toast } from 'sonner';

export function HistorySettings() {
  const [limit, setLimit] = useState(1000);
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    getHistoryLimit().then(setLimit).catch(console.error);
  }, []);

  const handleSave = async () => {
    setSaving(true);
    try {
      await setHistoryLimit(limit);
      toast.success('設定を保存しました');
    } catch (err) {
      toast.error('保存できませんでした。もう一度お試しください。');
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="space-y-4">
      <div>
        <label className="font-semibold text-xs text-foreground">履歴の保存件数</label>
        <div className="flex items-center gap-2 mt-1">
          <Input
            type="number"
            value={limit}
            onChange={(e) => setLimit(Number(e.target.value))}
            className="w-20 text-right text-sm"
            min={10}
            max={10000}
          />
          <span className="text-xs text-muted-foreground">件</span>
        </div>
        <p className="text-xs text-muted-foreground mt-1">
          制限を超えた場合、ピン留めされていない古い履歴から削除されます。
        </p>
      </div>
      <button
        onClick={handleSave}
        disabled={saving}
        className="bg-primary text-primary-foreground text-xs px-4 py-1.5 rounded-md disabled:opacity-50"
      >
        設定を保存
      </button>
    </div>
  );
}
```

**2. SettingsDialog.tsx 更新:**

import 追加:
```typescript
import { HistorySettings } from './HistorySettings';
```

TabsContent value="general" のプレースホルダーを置換:
```tsx
<TabsContent value="general" className="flex-1 overflow-y-auto m-0 p-4">
  <HistorySettings />
</TabsContent>
```

**3. StyleSelector.tsx — カスタムスタイル統合:**

import 追加:
```typescript
import { useCustomStyles } from '../hooks/useCustomStyles';
```

コンポーネント内で:
```typescript
const { data: customStyles } = useCustomStyles();

// ビルトインとカスタムを結合
const allStyles = [
  ...BUILT_IN_STYLES,
  ...(customStyles ?? []).map((s) => ({ id: s.id, label: s.label, emoji: s.emoji })),
];
```

JSX で `BUILT_IN_STYLES.map(...)` を `allStyles.map(...)` に変更。カスタムスタイルも同じチップスタイルで表示される。

**4. styles.ts にカスタムスタイル用のヘルパー追加:**

```typescript
/** ビルトインスタイルか判定 */
export function isBuiltInStyle(styleId: string): boolean {
  return BUILT_IN_STYLES.some((s) => s.id === styleId);
}
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/settings/HistorySettings.tsx exists
    - HistorySettings.tsx contains `getHistoryLimit` import
    - HistorySettings.tsx contains `setHistoryLimit` import
    - HistorySettings.tsx contains `設定を保存` button label
    - HistorySettings.tsx contains `履歴の保存件数` label
    - HistorySettings.tsx contains `type="number"` input
    - HistorySettings.tsx contains `保存できませんでした` error toast
    - src/components/settings/SettingsDialog.tsx contains `import { HistorySettings }`
    - src/components/settings/SettingsDialog.tsx contains `<HistorySettings />`
    - src/components/settings/SettingsDialog.tsx does NOT contain `全般設定は準備中` (placeholder removed)
    - src/components/StyleSelector.tsx contains `useCustomStyles` import
    - src/components/StyleSelector.tsx contains `allStyles` merged array
    - src/lib/styles.ts contains `export function isBuiltInStyle(`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>HistorySettings タブ完成、StyleSelector がカスタムスタイルを統合表示、全タブのプレースホルダー除去完了</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StyleManager form -> IPC | カスタムスタイルのプロンプトテキストが DB に保存される |
| HistorySettings -> IPC | 件数制限値が settings テーブルに保存される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Injection | Custom style prompt | accept | プロンプトは AI に渡されるテキスト。SQL はパラメータバインディングで安全。ユーザーが自分のプロンプトを書くのは意図通り |
| T-02-16 | Denial of Service | History limit = very small | accept | ユーザーが意図的に 10 に設定しても問題なし。ピン留めは保護。min=10 で 0 設定を防止 |
</threat_model>

<verification>
- `npx tsc --noEmit` 成功
- StyleManager が SettingsDialog の「スタイル」タブに表示される
- HistorySettings が「全般」タブに表示される
- StyleSelector がカスタムスタイルを表示する
</verification>

<success_criteria>
- TypeScript コンパイル成功
- StyleManager: カスタムスタイル一覧/追加/編集/削除 (インライン確認)
- HistorySettings: 件数制限入力 (デフォルト 1000) + 保存ボタン
- StyleSelector: ビルトイン 8 + カスタムスタイルが同じチップで表示
- SettingsDialog の全 3 タブが実コンポーネントに置換済み
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-driver-polish/02-06-SUMMARY.md`
</output>
