---
phase: 02-daily-driver-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tauri.ts
  - src/store/bufferStore.ts
  - src/store/settingsStore.ts
  - src/hooks/useHistory.ts
  - src/hooks/useProviders.ts
  - src/hooks/useCustomStyles.ts
autonomous: true
requirements:
  - HIST-04
  - HIST-05
  - HIST-06
  - HIST-07
  - HIST-08
  - BUFF-01
  - CONV-07
  - PROV-05
  - PROV-06

must_haves:
  truths:
    - "Wave 2 の全 UI プランが型エラーなしにコンパイルできる"
    - "bufferStore が items 配列と add/remove/reorder/copyAll アクションを持つ"
    - "useHistory が search/styleFilter パラメータを受け取る"
    - "useProviders が config 取得・upsert・delete・ping の mutation を持つ"
  artifacts:
    - path: "src/lib/tauri.ts"
      provides: "全新規 invoke ラッパー"
      exports: ["searchHistory", "pinHistory", "deleteHistory", "setHistoryLimit", "getHistoryLimit", "listStyles", "createStyle", "updateStyle", "deleteStyle", "getProviderConfig", "upsertProvider", "deleteProvider", "pingProvider", "setActiveProvider"]
    - path: "src/store/bufferStore.ts"
      provides: "ドラフトバッファ Zustand store"
      exports: ["useBufferStore"]
    - path: "src/hooks/useCustomStyles.ts"
      provides: "カスタムスタイル TanStack Query hooks"
      exports: ["useCustomStyles"]
  key_links:
    - from: "src/hooks/useHistory.ts"
      to: "src/lib/tauri.ts"
      via: "searchHistory/getHistory invoke calls"
      pattern: "searchHistory|getHistory"
    - from: "src/store/bufferStore.ts"
      to: "@dnd-kit/sortable"
      via: "arrayMove import"
      pattern: "arrayMove"
---

<objective>
Phase 2 のフロントエンド基盤を構築する: 全新規 Tauri コマンドの invoke ラッパー、bufferStore、hooks 拡張。

Purpose: Wave 2 の UI プラン (03, 04, 05, 06) がこれらの型とラッパーに依存する。先にコントラクトを定義することで UI 実装が型安全に進められる。
Output: 全 invoke ラッパー、bufferStore、拡張 hooks が TypeScript コンパイル可能な状態。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-daily-driver-polish/02-RESEARCH.md

<interfaces>
From src/lib/tauri.ts (Phase 1):
```typescript
export interface ConvertResult { converted: string; intent: string; typo: string; historyId: number; }
export interface ConversionRecord { id: number; input: string; output: string; styleId: string; intent: string | null; typo: string | null; providerId: string; model: string; pinned: boolean; createdAt: string; }
export interface ProviderInfo { id: string; name: string; providerType: string; }
export interface WindowState { x: number; y: number; width: number; height: number; }
export async function convertText(input, styleId, providerId): Promise<ConvertResult>;
export async function getHistory(limit, offset, styleFilter?): Promise<ConversionRecord[]>;
export async function listProviders(): Promise<ProviderInfo[]>;
```

From src/store/settingsStore.ts:
```typescript
interface SettingsState { theme: 'dark'|'light'; alwaysOnTop: boolean; activeProviderId: string; ... }
```

From src/lib/styles.ts:
```typescript
export interface StylePreset { id: string; label: string; emoji: string; }
export const BUILT_IN_STYLES: StylePreset[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tauri invoke ラッパー + 新規型定義</name>
  <files>src/lib/tauri.ts</files>
  <read_first>
    src/lib/tauri.ts,
    src-tauri/src/commands/history.rs,
    src-tauri/src/commands/styles.rs,
    src-tauri/src/commands/providers.rs,
    src-tauri/src/db/custom_styles.rs,
    src-tauri/src/providers/mod.rs
  </read_first>
  <action>
**src/lib/tauri.ts に以下の型と関数を追加:**

既存コードはすべて残す。ファイル末尾に追加:

**新規型:**
```typescript
export interface CustomStyle {
  id: string;
  label: string;
  emoji: string;
  prompt: string;
  sortOrder: number;
  createdAt: string;
}

export interface ProviderConfig {
  id: string;
  name: string;
  adapter: string;
  baseUrl: string | null;
  apiKey: string | null;
  model: string;
  enabled: boolean;
}

export interface ProvidersFile {
  providers: ProviderConfig[];
  defaultProvider: string;
}
```

**履歴コマンド:**
```typescript
export async function searchHistory(
  query: string, styleFilter: string | null, limit: number, offset: number,
): Promise<ConversionRecord[]> {
  return invoke<ConversionRecord[]>('search_history', { query, styleFilter, limit, offset });
}

export async function pinHistory(id: number, pinned: boolean): Promise<void> {
  return invoke('pin_history', { id, pinned });
}

export async function deleteHistory(id: number): Promise<void> {
  return invoke('delete_history', { id });
}

export async function setHistoryLimit(limit: number): Promise<void> {
  return invoke('set_history_limit', { limit });
}

export async function getHistoryLimit(): Promise<number> {
  return invoke<number>('get_history_limit');
}
```

**スタイルコマンド:**
```typescript
export async function listCustomStyles(): Promise<CustomStyle[]> {
  return invoke<CustomStyle[]>('list_styles');
}

export async function createCustomStyle(
  id: string, label: string, emoji: string, prompt: string, sortOrder: number,
): Promise<void> {
  return invoke('create_style', { id, label, emoji, prompt, sortOrder });
}

export async function updateCustomStyle(
  id: string, label: string, emoji: string, prompt: string,
): Promise<void> {
  return invoke('update_style', { id, label, emoji, prompt });
}

export async function deleteCustomStyle(id: string): Promise<void> {
  return invoke('delete_style', { id });
}
```

**プロバイダーコマンド:**
```typescript
export async function getProviderConfig(): Promise<ProvidersFile> {
  return invoke<ProvidersFile>('get_provider_config');
}

export async function upsertProvider(config: ProviderConfig): Promise<void> {
  return invoke('upsert_provider', { config });
}

export async function deleteProvider(providerId: string): Promise<void> {
  return invoke('delete_provider', { providerId });
}

export async function pingProvider(baseUrl: string, apiKey: string | null): Promise<string> {
  return invoke<string>('ping_provider', { baseUrl, apiKey });
}

export async function setActiveProvider(providerId: string): Promise<void> {
  return invoke('set_active_provider', { providerId });
}
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/tauri.ts contains `export interface CustomStyle`
    - src/lib/tauri.ts contains `export interface ProviderConfig`
    - src/lib/tauri.ts contains `export interface ProvidersFile`
    - src/lib/tauri.ts contains `export async function searchHistory(`
    - src/lib/tauri.ts contains `export async function pinHistory(`
    - src/lib/tauri.ts contains `export async function deleteHistory(`
    - src/lib/tauri.ts contains `export async function setHistoryLimit(`
    - src/lib/tauri.ts contains `export async function getHistoryLimit(`
    - src/lib/tauri.ts contains `export async function listCustomStyles(`
    - src/lib/tauri.ts contains `export async function createCustomStyle(`
    - src/lib/tauri.ts contains `export async function updateCustomStyle(`
    - src/lib/tauri.ts contains `export async function deleteCustomStyle(`
    - src/lib/tauri.ts contains `export async function getProviderConfig(`
    - src/lib/tauri.ts contains `export async function upsertProvider(`
    - src/lib/tauri.ts contains `export async function deleteProvider(`
    - src/lib/tauri.ts contains `export async function pingProvider(`
    - src/lib/tauri.ts contains `export async function setActiveProvider(`
    - `npx tsc --noEmit` passes (or no errors in tauri.ts)
  </acceptance_criteria>
  <done>14 個の新規 invoke ラッパーと 3 つの新規型が tauri.ts に追加済み</done>
</task>

<task type="auto">
  <name>Task 2: bufferStore + hooks 拡張 (useHistory, useProviders, useCustomStyles)</name>
  <files>
    src/store/bufferStore.ts,
    src/store/settingsStore.ts,
    src/hooks/useHistory.ts,
    src/hooks/useProviders.ts,
    src/hooks/useCustomStyles.ts
  </files>
  <read_first>
    src/store/settingsStore.ts,
    src/hooks/useHistory.ts,
    src/hooks/useProviders.ts,
    src/lib/tauri.ts
  </read_first>
  <action>
**1. 新規: src/store/bufferStore.ts**

```typescript
import { create } from 'zustand';
import { arrayMove } from '@dnd-kit/sortable';

export interface BufferItem {
  id: string;
  text: string;
  styleId: string;
  createdAt: number;
}

interface BufferStore {
  items: BufferItem[];
  addItem: (text: string, styleId: string) => void;
  removeItem: (id: string) => void;
  reorder: (oldIndex: number, newIndex: number) => void;
  copyAll: () => string;
  clear: () => void;
}

export const useBufferStore = create<BufferStore>((set, get) => ({
  items: [],
  addItem: (text, styleId) =>
    set((s) => ({
      items: [...s.items, { id: crypto.randomUUID(), text, styleId, createdAt: Date.now() }],
    })),
  removeItem: (id) => set((s) => ({ items: s.items.filter((i) => i.id !== id) })),
  reorder: (oldIndex, newIndex) =>
    set((s) => ({ items: arrayMove(s.items, oldIndex, newIndex) })),
  copyAll: () => get().items.map((i) => i.text).join('\n\n'),
  clear: () => set({ items: [] }),
}));
```

**2. settingsStore.ts に historyLimit 追加:**

既存のインターフェースに追加:
```typescript
historyLimit: number;
setHistoryLimit: (limit: number) => void;
```

store 内に:
```typescript
historyLimit: 1000,
setHistoryLimit: (limit) => set({ historyLimit: limit }),
```

**3. hooks/useHistory.ts を拡張:**

```typescript
import { useQuery } from '@tanstack/react-query';
import { getHistory, searchHistory } from '../lib/tauri';

interface UseHistoryOptions {
  enabled?: boolean;
  search?: string;
  styleFilter?: string | null;
  limit?: number;
  offset?: number;
}

export function useHistory(options: UseHistoryOptions = {}) {
  const {
    enabled = true,
    search = '',
    styleFilter = null,
    limit = 50,
    offset = 0,
  } = options;

  return useQuery({
    queryKey: ['history', search, styleFilter, limit, offset],
    queryFn: () => {
      if (search.trim()) {
        return searchHistory(search.trim(), styleFilter, limit, offset);
      }
      return getHistory(limit, offset, styleFilter ?? undefined);
    },
    enabled,
    staleTime: 10_000,
  });
}
```

注意: useHistory の既存呼び出し元 (HistoryDrawer.tsx) は引数シグネチャが変わるので、HistoryDrawer.tsx は Plan 03 で更新する。破壊的変更を避けるため、引数なし呼び出し `useHistory()` もデフォルト値で動作するようにする（上記のデフォルトパラメータで対応済み）。

ただし Phase 1 の `useHistory(isDrawerOpen)` 呼び出しも互換維持が必要。上記実装ではオブジェクト引数に変更しているため、HistoryDrawer.tsx での呼び出しを `useHistory({ enabled: isDrawerOpen })` に変更する必要がある。しかし HistoryDrawer.tsx は Plan 03 の担当ファイルなので、**このタスクでは HistoryDrawer.tsx の呼び出しも修正する** (1 行変更のみ):

`src/components/HistoryDrawer.tsx` の行 18:
```typescript
// 変更前: const { data: history, isLoading } = useHistory(isDrawerOpen);
// 変更後:
const { data: history, isLoading } = useHistory({ enabled: isDrawerOpen });
```

**4. hooks/useProviders.ts を拡張:**

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import {
  listProviders,
  getProviderConfig,
  upsertProvider,
  deleteProvider,
  pingProvider,
  setActiveProvider,
} from '../lib/tauri';
import type { ProviderConfig } from '../lib/tauri';

export function useProviders() {
  return useQuery({
    queryKey: ['providers'],
    queryFn: listProviders,
    staleTime: Infinity,
  });
}

export function useProviderConfig() {
  return useQuery({
    queryKey: ['providerConfig'],
    queryFn: getProviderConfig,
    staleTime: 30_000,
  });
}

export function useUpsertProvider() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (config: ProviderConfig) => upsertProvider(config),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['providerConfig'] });
      queryClient.invalidateQueries({ queryKey: ['providers'] });
    },
  });
}

export function useDeleteProvider() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (providerId: string) => deleteProvider(providerId),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['providerConfig'] });
      queryClient.invalidateQueries({ queryKey: ['providers'] });
    },
  });
}

export function usePingProvider() {
  return useMutation({
    mutationFn: ({ baseUrl, apiKey }: { baseUrl: string; apiKey: string | null }) =>
      pingProvider(baseUrl, apiKey),
  });
}

export function useSetActiveProvider() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (providerId: string) => setActiveProvider(providerId),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['providerConfig'] });
    },
  });
}
```

**5. 新規: src/hooks/useCustomStyles.ts**

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import {
  listCustomStyles,
  createCustomStyle,
  updateCustomStyle,
  deleteCustomStyle,
} from '../lib/tauri';

export function useCustomStyles() {
  return useQuery({
    queryKey: ['customStyles'],
    queryFn: listCustomStyles,
    staleTime: 30_000,
  });
}

export function useCreateStyle() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (args: { id: string; label: string; emoji: string; prompt: string; sortOrder: number }) =>
      createCustomStyle(args.id, args.label, args.emoji, args.prompt, args.sortOrder),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['customStyles'] }),
  });
}

export function useUpdateStyle() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (args: { id: string; label: string; emoji: string; prompt: string }) =>
      updateCustomStyle(args.id, args.label, args.emoji, args.prompt),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['customStyles'] }),
  });
}

export function useDeleteStyle() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteCustomStyle(id),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['customStyles'] }),
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/store/bufferStore.ts exists and contains `export const useBufferStore`
    - src/store/bufferStore.ts contains `arrayMove` import from `@dnd-kit/sortable`
    - src/store/bufferStore.ts contains `export interface BufferItem`
    - src/store/settingsStore.ts contains `historyLimit: number`
    - src/store/settingsStore.ts contains `setHistoryLimit`
    - src/hooks/useHistory.ts contains `interface UseHistoryOptions`
    - src/hooks/useHistory.ts contains `searchHistory(search.trim()`
    - src/hooks/useHistory.ts contains `queryKey: ['history', search, styleFilter`
    - src/components/HistoryDrawer.tsx contains `useHistory({ enabled: isDrawerOpen })`
    - src/hooks/useProviders.ts contains `export function useProviderConfig(`
    - src/hooks/useProviders.ts contains `export function useUpsertProvider(`
    - src/hooks/useProviders.ts contains `export function useDeleteProvider(`
    - src/hooks/useProviders.ts contains `export function usePingProvider(`
    - src/hooks/useProviders.ts contains `export function useSetActiveProvider(`
    - src/hooks/useCustomStyles.ts exists and contains `export function useCustomStyles(`
    - src/hooks/useCustomStyles.ts contains `export function useCreateStyle(`
    - src/hooks/useCustomStyles.ts contains `export function useUpdateStyle(`
    - src/hooks/useCustomStyles.ts contains `export function useDeleteStyle(`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>bufferStore 作成、settingsStore 拡張、useHistory/useProviders 拡張、useCustomStyles 新規作成、HistoryDrawer 互換対応完了</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend stores -> Tauri IPC | invoke ラッパーがユーザー入力をコマンドに渡す |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | ProviderConfig type | mitigate | apiKey フィールドは `"<encrypted>"` のまま返る。フロントエンドで実キーを表示しない (password masking は Plan 05 で実装) |
| T-02-07 | Tampering | bufferStore | accept | ドラフトバッファはメモリ内のみ。永続化しないため改ざんリスクなし |
</threat_model>

<verification>
- `npx tsc --noEmit` が成功する
- 全 invoke ラッパーが tauri.ts にエクスポートされている
- bufferStore が arrayMove を使った reorder を持つ
- useHistory が検索・フィルター対応
</verification>

<success_criteria>
- TypeScript コンパイル成功
- 14 個の新規 invoke ラッパーが tauri.ts に存在
- bufferStore (Zustand) が items/addItem/removeItem/reorder/copyAll/clear を持つ
- useHistory がオブジェクト引数で search/styleFilter/limit/offset を受け取る
- useProviders が config/upsert/delete/ping/setActive の hooks を持つ
- useCustomStyles が list/create/update/delete の hooks を持つ
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-driver-polish/02-02-SUMMARY.md`
</output>
