---
phase: 02-daily-driver-polish
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/components/HistoryDrawer.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
autonomous: true
requirements:
  - HIST-04
  - HIST-05
  - HIST-06
  - HIST-07

must_haves:
  truths:
    - "履歴検索入力に 300ms デバウンスで FTS5 検索が走る"
    - "スタイルフィルターチップで特定スタイルの履歴のみ表示できる"
    - "ピンアイコンをクリックするとピン状態がトグルされ、ピン留め項目が先頭に表示される"
    - "履歴アイテムにホバーすると削除ボタンが表示され、即座に削除できる"
    - "検索結果が 0 件のとき「見つかりませんでした」が表示される"
  artifacts:
    - path: "src/components/HistoryDrawer.tsx"
      provides: "検索・フィルター・ピン・削除 UI 拡張版"
      min_lines: 150
    - path: "src/components/ui/input.tsx"
      provides: "shadcn Input コンポーネント"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge コンポーネント"
  key_links:
    - from: "src/components/HistoryDrawer.tsx"
      to: "src/hooks/useHistory.ts"
      via: "useHistory({ search, styleFilter, enabled })"
      pattern: "useHistory\\("
    - from: "src/components/HistoryDrawer.tsx"
      to: "src/lib/tauri.ts"
      via: "pinHistory/deleteHistory invoke"
      pattern: "pinHistory|deleteHistory"
---

<objective>
HistoryDrawer を検索・フィルター・ピン・削除に対応した完全版に拡張する。

Purpose: ユーザーが過去の変換を素早く見つけ、頻繁に使うものをピン留めし、不要なものを削除できるようにする。
Output: 既存 HistoryDrawer.tsx の拡張版。shadcn Input/Badge コンポーネント追加。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-daily-driver-polish/02-UI-SPEC.md
@.planning/phases/02-daily-driver-polish/02-RESEARCH.md
@.planning/phases/02-daily-driver-polish/02-02-SUMMARY.md

<interfaces>
From src/hooks/useHistory.ts (Plan 02 で拡張済み):
```typescript
interface UseHistoryOptions {
  enabled?: boolean;
  search?: string;
  styleFilter?: string | null;
  limit?: number;
  offset?: number;
}
export function useHistory(options?: UseHistoryOptions);
```

From src/lib/tauri.ts (Plan 02 で追加済み):
```typescript
export async function pinHistory(id: number, pinned: boolean): Promise<void>;
export async function deleteHistory(id: number): Promise<void>;
export interface ConversionRecord {
  id: number; input: string; output: string; styleId: string;
  intent: string | null; typo: string | null;
  providerId: string; model: string; pinned: boolean; createdAt: string;
}
```

From src/lib/styles.ts:
```typescript
export interface StylePreset { id: string; label: string; emoji: string; }
export const BUILT_IN_STYLES: StylePreset[];
```

From src/store/historyStore.ts:
```typescript
interface HistoryState { isDrawerOpen: boolean; toggleDrawer: () => void; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn Input/Badge コンポーネント追加</name>
  <files>src/components/ui/input.tsx, src/components/ui/badge.tsx</files>
  <read_first>
    src/components/ui/button.tsx
  </read_first>
  <action>
shadcn CLI で Input と Badge を追加:
```bash
npx shadcn@latest add input badge
```

これにより `src/components/ui/input.tsx` と `src/components/ui/badge.tsx` が生成される。shadcn は `components.json` の設定を読んで Tailwind v4 対応コードを出力する。
  </action>
  <verify>
    <automated>ls -la /Users/ohya/workspaces/romaji-editor/src/components/ui/input.tsx /Users/ohya/workspaces/romaji-editor/src/components/ui/badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/input.tsx exists
    - src/components/ui/badge.tsx exists
    - Both files contain `export` statements
  </acceptance_criteria>
  <done>shadcn Input/Badge コンポーネントがインストール済み</done>
</task>

<task type="auto">
  <name>Task 2: HistoryDrawer 拡張 — 検索、スタイルフィルター、ピン、削除</name>
  <files>src/components/HistoryDrawer.tsx</files>
  <read_first>
    src/components/HistoryDrawer.tsx,
    src/hooks/useHistory.ts,
    src/lib/tauri.ts,
    src/lib/styles.ts,
    src/store/historyStore.ts,
    src/store/conversionStore.ts,
    src/components/ui/input.tsx,
    src/components/ui/badge.tsx,
    .planning/phases/02-daily-driver-polish/02-UI-SPEC.md
  </read_first>
  <action>
HistoryDrawer.tsx を以下の仕様で全面書き換え。既存の構造（toggle button + drawer content + window resize）は維持しつつ、機能を追加する。

**1. 状態管理追加:**
```typescript
const [searchQuery, setSearchQuery] = useState('');
const [debouncedSearch, setDebouncedSearch] = useState('');
const [styleFilter, setStyleFilter] = useState<string | null>(null);
```

**2. 検索デバウンス (300ms) — UI-SPEC 準拠:**
```typescript
useEffect(() => {
  const timer = setTimeout(() => setDebouncedSearch(searchQuery), 300);
  return () => clearTimeout(timer);
}, [searchQuery]);
```

**3. useHistory 呼び出し変更:**
```typescript
const { data: history, isLoading } = useHistory({
  enabled: isDrawerOpen,
  search: debouncedSearch,
  styleFilter,
});
```

**4. Drawer content 内部の構造 (上から下):**

a) **検索バー (HIST-04):**
- `<div className="px-3 pt-2 pb-1">` で囲む
- `<div className="relative">` + `<Search size={12} className="absolute left-2.5 top-1/2 -translate-y-1/2 text-muted-foreground" />` + `<Input placeholder="キーワード検索..." className="h-8 pl-8 text-xs" value={searchQuery} onChange={...} />`
- 検索中に 500ms 以上経過時: `{isLoading && debouncedSearch && <span className="text-xs text-muted-foreground">検索中...</span>}`

b) **スタイルフィルターチップ (HIST-05):**
- `<div className="flex gap-1 px-3 py-1.5 overflow-x-auto">` — 横スクロール
- 「すべて」チップ (null フィルター) + BUILT_IN_STYLES の各スタイル
- アクティブ: `<Badge className="cursor-pointer bg-primary text-primary-foreground hover:bg-primary/90 text-[10px] px-1.5 py-0.5">`
- 非アクティブ: `<Badge variant="secondary" className="cursor-pointer text-[10px] px-1.5 py-0.5">`
- onClick で `setStyleFilter(style.id)` / `setStyleFilter(null)` をトグル

c) **履歴リスト:**
- 既存のリスト構造を維持しつつ、各アイテムに以下を追加:

d) **ピンアイコン (HIST-06):**
- 各アイテム行の右側、タイムスタンプの左に配置
- `<button onClick={(e) => { e.stopPropagation(); handlePin(item.id, !item.pinned); }} className="p-0.5">`
- ピン済み: `<Pin size={12} className="text-primary fill-primary" />`
- 未ピン: `<Pin size={12} className="text-muted-foreground" />`
- `handlePin` は `pinHistory(id, pinned)` を呼び、成功後に `queryClient.invalidateQueries({ queryKey: ['history'] })` で楽観的更新

e) **削除ボタン (HIST-07):**
- 各アイテム行の最右端、ホバー時のみ表示
- `<button onClick={(e) => { e.stopPropagation(); handleDelete(item.id); }} className="p-0.5 opacity-0 group-hover:opacity-100 transition-opacity">`
- `<Trash2 size={12} className="text-muted-foreground hover:text-destructive" />`
- `handleDelete` は `deleteHistory(id)` を呼び、成功後に `queryClient.invalidateQueries({ queryKey: ['history'] })`
- UI-SPEC: 確認なし、即削除

f) **各アイテム行に `group` クラスを追加:**
```typescript
<div key={item.id} className="group w-full text-left px-3 py-2 hover:bg-muted/50 transition-colors cursor-pointer" onClick={...}>
```
（button から div に変更し、内部のクリック可能エリアを分ける）

**5. 空状態 (UI-SPEC Copywriting Contract 準拠):**
- 履歴なし: heading "履歴がありません" + body "変換すると、ここに表示されます。"
- 検索結果なし: heading "見つかりませんでした" + body "別のキーワードで試してください。"

**6. import 追加:**
- `import { Search, Pin, Trash2 } from 'lucide-react'` (ChevronDown, ChevronUp, Clock は既存)
- `import { Input } from './ui/input'`
- `import { Badge } from './ui/badge'`
- `import { pinHistory, deleteHistory } from '../lib/tauri'`
- `import { useState } from 'react'` (useEffect, useRef は既存)
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/HistoryDrawer.tsx contains `import { Input }` from ui/input
    - src/components/HistoryDrawer.tsx contains `import { Badge }` from ui/badge
    - src/components/HistoryDrawer.tsx contains `placeholder="キーワード検索..."`
    - src/components/HistoryDrawer.tsx contains `setDebouncedSearch` (300ms debounce)
    - src/components/HistoryDrawer.tsx contains `setTimeout` with `300`
    - src/components/HistoryDrawer.tsx contains `styleFilter` state
    - src/components/HistoryDrawer.tsx contains `"すべて"` as filter chip label
    - src/components/HistoryDrawer.tsx contains `pinHistory(` call
    - src/components/HistoryDrawer.tsx contains `deleteHistory(` call
    - src/components/HistoryDrawer.tsx contains `fill-primary` (pinned icon style)
    - src/components/HistoryDrawer.tsx contains `group-hover:opacity-100` (delete button hover)
    - src/components/HistoryDrawer.tsx contains `見つかりませんでした` (empty search state)
    - src/components/HistoryDrawer.tsx contains `履歴がありません` (empty history state)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>HistoryDrawer が検索 (300ms debounce + FTS5)、スタイルフィルター、ピントグル (楽観的更新)、ホバー削除に対応</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search input -> Tauri IPC | 検索クエリがそのまま FTS5 MATCH に渡される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Injection | Search input | mitigate | Rust 側で `query.replace('"', "")` + パラメータバインディング (Plan 01 で実装済み)。フロントは trim のみ |
| T-02-09 | Denial of Service | Debounce bypass | accept | 300ms debounce はベストエフォート。ユーザーがデバウンスを回避しても FTS5 は高速 (trigram index) |
</threat_model>

<verification>
- `npx tsc --noEmit` 成功
- HistoryDrawer に検索バー、フィルターチップ、ピン、削除が存在
- shadcn Input/Badge コンポーネントがインストール済み
</verification>

<success_criteria>
- TypeScript コンパイル成功
- 検索入力 (placeholder: "キーワード検索...") が描画される
- スタイルフィルターチップ (「すべて」+ 8 スタイル) が描画される
- ピンアイコンが各履歴アイテムに表示される
- 削除ボタンがホバー時に表示される
- 空状態メッセージが UI-SPEC の Copywriting Contract と一致する
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-driver-polish/02-03-SUMMARY.md`
</output>
