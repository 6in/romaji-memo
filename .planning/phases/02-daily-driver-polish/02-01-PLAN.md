---
phase: 02-daily-driver-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/src/lib.rs
  - src-tauri/src/db/mod.rs
  - src-tauri/src/db/migrations.rs
  - src-tauri/src/db/conversions.rs
  - src-tauri/src/db/custom_styles.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/history.rs
  - src-tauri/src/commands/providers.rs
  - src-tauri/src/commands/styles.rs
  - src-tauri/src/providers/mod.rs
  - package.json
autonomous: true
requirements:
  - WINX-02
  - HIST-04
  - HIST-06
  - HIST-07
  - HIST-08
  - CONV-07
  - PROV-05
  - PROV-06
  - PROV-08

must_haves:
  truths:
    - "Cmd/Ctrl+Shift+R toggles window visibility even when window is hidden"
    - "FTS5 trigram search returns partial matches for Japanese and romaji text 3+ characters"
    - "History items can be pinned/unpinned, deleted, and searched from backend"
    - "Custom styles can be created, read, updated, deleted from backend"
    - "Providers can be upserted, deleted, pinged from backend"
    - "All new Tauri commands are registered in invoke_handler"
  artifacts:
    - path: "src-tauri/src/db/migrations.rs"
      provides: "MIGRATION_002 with FTS5 trigram"
      contains: "MIGRATION_002"
    - path: "src-tauri/src/db/custom_styles.rs"
      provides: "Custom style CRUD functions"
      exports: ["insert_custom_style", "get_custom_styles", "update_custom_style", "delete_custom_style"]
    - path: "src-tauri/src/commands/styles.rs"
      provides: "Tauri commands for custom styles"
      exports: ["create_style", "list_styles", "update_style", "delete_style"]
    - path: "src-tauri/src/commands/providers.rs"
      provides: "Provider management commands"
      exports: ["list_providers", "upsert_provider", "delete_provider", "ping_provider", "get_provider_config", "set_active_provider"]
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "tauri_plugin_global_shortcut"
      via: "plugin registration in setup()"
      pattern: "tauri_plugin_global_shortcut::Builder"
    - from: "src-tauri/src/db/mod.rs"
      to: "src-tauri/src/db/migrations.rs"
      via: "run_migrations calls MIGRATION_002"
      pattern: "MIGRATION_002"
---

<objective>
Phase 2 の Rust バックエンド基盤をすべて構築する: グローバルショートカット、FTS5 trigram マイグレーション、履歴検索/ピン/削除コマンド、カスタムスタイル CRUD、プロバイダー管理コマンド。

Purpose: Wave 2 のフロントエンド UI プランがすべてこのバックエンドに依存する。先にコマンド層を完成させることで、UI プランが独立並列実行できる。
Output: 全ての新規 Tauri コマンドが登録済みで `cargo build` が通る状態。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-daily-driver-polish/02-RESEARCH.md

<interfaces>
<!-- Phase 1 の既存インターフェース: executor はこれらを参照すること -->

From src-tauri/src/state.rs:
```rust
pub struct AppState {
    pub db: Arc<tokio::sync::Mutex<Connection>>,
    pub providers: RwLock<HashMap<String, Arc<dyn ProviderAdapter + Send + Sync>>>,
    pub keychain_lock: Mutex<()>,
}
```

From src-tauri/src/providers/mod.rs:
```rust
pub struct ProviderConfig {
    pub id: String,
    pub name: String,
    pub adapter: String,
    pub base_url: Option<String>,
    pub api_key: Option<String>,
    pub model: String,
    pub enabled: bool,
}

pub struct ProvidersFile {
    pub providers: Vec<ProviderConfig>,
    pub default_provider: String,
}

pub trait ProviderAdapter: Send + Sync {
    async fn complete(&self, req: CompletionRequest) -> Result<CompletionResponse, ProviderError>;
    fn name(&self) -> &str;
    fn provider_type(&self) -> &str;
    fn model_id(&self) -> &str;
}

pub fn load_providers_config(app_data_dir: &Path) -> Result<ProvidersFile, String>;
```

From src-tauri/src/db/conversions.rs:
```rust
pub struct ConversionRecord {
    pub id: i64, pub input: String, pub output: String, pub style_id: String,
    pub intent: Option<String>, pub typo: Option<String>,
    pub provider_id: String, pub model: String, pub pinned: bool, pub created_at: String,
}
fn map_row(row: &Row) -> Result<ConversionRecord, rusqlite::Error>;
```

From src-tauri/src/keychain.rs:
```rust
pub fn get_api_key(keychain_lock: &Mutex<()>, provider_id: &str) -> Result<Option<String>, String>;
pub fn set_api_key(keychain_lock: &Mutex<()>, provider_id: &str, api_key: &str) -> Result<(), String>;
```

From src-tauri/src/db/settings.rs:
```rust
pub fn get_setting(conn: &Connection, key: &str) -> Result<Option<String>, rusqlite::Error>;
pub fn set_setting(conn: &Connection, key: &str, value: &str) -> Result<(), rusqlite::Error>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 依存関係インストール + グローバルショートカット + FTS5 trigram マイグレーション</name>
  <files>
    src-tauri/Cargo.toml,
    src-tauri/capabilities/default.json,
    src-tauri/src/lib.rs,
    src-tauri/src/db/migrations.rs,
    src-tauri/src/db/mod.rs,
    package.json
  </files>
  <read_first>
    src-tauri/Cargo.toml,
    src-tauri/capabilities/default.json,
    src-tauri/src/lib.rs,
    src-tauri/src/db/migrations.rs,
    src-tauri/src/db/mod.rs,
    package.json,
    .planning/phases/02-daily-driver-polish/02-RESEARCH.md
  </read_first>
  <action>
**1. Cargo.toml に依存追加:**

`[dependencies]` の `tauri-plugin-clipboard-manager` の下に追加:
```toml
[target."cfg(not(any(target_os = \"android\", target_os = \"ios\")))".dependencies]
tauri-plugin-global-shortcut = "2"
```

**2. package.json に npm 依存追加:**
```bash
npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/utilities@3.2.2 @tauri-apps/plugin-global-shortcut@2.3.1
```

**3. capabilities/default.json の permissions 配列に追加:**
```json
"global-shortcut:allow-is-registered",
"global-shortcut:allow-register",
"global-shortcut:allow-unregister"
```

**4. src-tauri/src/lib.rs の `setup()` 内にグローバルショートカット登録を追加:**

`run()` 関数内の `tauri::Builder::default()` チェーンで `.plugin(tauri_plugin_clipboard_manager::init())` の直後に:
```rust
.plugin({
    use tauri_plugin_global_shortcut::{Code, GlobalShortcutExt, Modifiers, Shortcut, ShortcutState};
    let shortcut = Shortcut::new(Some(Modifiers::SUPER | Modifiers::SHIFT), Code::KeyR);
    tauri_plugin_global_shortcut::Builder::new()
        .with_handler(move |app, registered_shortcut, event| {
            if registered_shortcut == &shortcut && event.state() == ShortcutState::Pressed {
                if let Some(window) = app.get_webview_window("main") {
                    match window.is_visible() {
                        Ok(true) => { let _ = window.hide(); }
                        _ => {
                            let _ = window.show();
                            let _ = window.set_focus();
                            let _ = window.set_always_on_top(true);
                        }
                    }
                }
            }
        })
        .build()
})
```
`setup()` クロージャ内（`app.manage(app_state)` の後）でショートカットを登録:
```rust
{
    use tauri_plugin_global_shortcut::{Code, GlobalShortcutExt, Modifiers, Shortcut};
    let shortcut = Shortcut::new(Some(Modifiers::SUPER | Modifiers::SHIFT), Code::KeyR);
    app.global_shortcut().register(shortcut)?;
}
```

ファイル先頭の use 文に追加: `use tauri_plugin_global_shortcut;` (必要なら)。

**5. FTS5 trigram マイグレーション (MIGRATION_002):**

`src-tauri/src/db/migrations.rs` に `MIGRATION_002` を追加:
```rust
pub const MIGRATION_002: &str = "
BEGIN;
DROP TRIGGER IF EXISTS conversions_au;
DROP TRIGGER IF EXISTS conversions_ad;
DROP TRIGGER IF EXISTS conversions_ai;
DROP TABLE IF EXISTS conversions_fts;

CREATE VIRTUAL TABLE conversions_fts USING fts5(
  input, output,
  content='conversions', content_rowid='id',
  tokenize='trigram'
);

INSERT INTO conversions_fts(conversions_fts) VALUES('rebuild');

CREATE TRIGGER conversions_ai AFTER INSERT ON conversions BEGIN
  INSERT INTO conversions_fts(rowid, input, output)
  VALUES (new.id, new.input, new.output);
END;
CREATE TRIGGER conversions_ad AFTER DELETE ON conversions BEGIN
  INSERT INTO conversions_fts(conversions_fts, rowid, input, output)
  VALUES ('delete', old.id, old.input, old.output);
END;
CREATE TRIGGER conversions_au AFTER UPDATE ON conversions BEGIN
  INSERT INTO conversions_fts(conversions_fts, rowid, input, output)
  VALUES ('delete', old.id, old.input, old.output);
  INSERT INTO conversions_fts(rowid, input, output)
  VALUES (new.id, new.input, new.output);
END;
COMMIT;
";
```

**6. db/mod.rs の `run_migrations()` に MIGRATION_002 適用ロジック追加:**
```rust
if version < 2 {
    conn.execute_batch(migrations::MIGRATION_002)?;
    conn.execute_batch("PRAGMA user_version = 2;")?;
}
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/Cargo.toml contains `tauri-plugin-global-shortcut = "2"`
    - src-tauri/capabilities/default.json contains `global-shortcut:allow-register`
    - src-tauri/src/lib.rs contains `tauri_plugin_global_shortcut::Builder::new()`
    - src-tauri/src/lib.rs contains `app.global_shortcut().register(shortcut)`
    - src-tauri/src/db/migrations.rs contains `pub const MIGRATION_002`
    - src-tauri/src/db/migrations.rs contains `tokenize='trigram'`
    - src-tauri/src/db/mod.rs contains `version < 2`
    - package.json contains `@dnd-kit/core`
    - package.json contains `@tauri-apps/plugin-global-shortcut`
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
  <done>グローバルショートカット登録済み、FTS5 trigram マイグレーション定義済み、全依存インストール済み、cargo build が通る</done>
</task>

<task type="auto">
  <name>Task 2: 履歴バックエンド拡張 — 検索、ピン、削除、件数制限</name>
  <files>
    src-tauri/src/db/conversions.rs,
    src-tauri/src/commands/history.rs
  </files>
  <read_first>
    src-tauri/src/db/conversions.rs,
    src-tauri/src/commands/history.rs,
    src-tauri/src/state.rs,
    src-tauri/src/db/settings.rs
  </read_first>
  <action>
**1. db/conversions.rs に関数追加:**

既存の `get_history` の ORDER BY を `pinned DESC, created_at DESC` に変更 (ピン留めが先頭に来るように)。

`search_history` 関数を追加:
```rust
pub fn search_history(
    conn: &Connection,
    query: &str,
    style_filter: Option<&str>,
    limit: i64,
    offset: i64,
) -> Result<Vec<ConversionRecord>, rusqlite::Error> {
    // trigram は 3 文字未満にマッチしないので LIKE フォールバック
    if query.chars().count() < 3 {
        let like_pattern = format!("%{}%", query);
        if let Some(style) = style_filter {
            let mut stmt = conn.prepare(
                "SELECT id, input, output, style_id, intent, typo, provider_id, model, pinned, created_at
                 FROM conversions
                 WHERE (input LIKE ?1 OR output LIKE ?1) AND style_id = ?2
                 ORDER BY pinned DESC, created_at DESC
                 LIMIT ?3 OFFSET ?4"
            )?;
            stmt.query_map(rusqlite::params![like_pattern, style, limit, offset], map_row)?.collect()
        } else {
            let mut stmt = conn.prepare(
                "SELECT id, input, output, style_id, intent, typo, provider_id, model, pinned, created_at
                 FROM conversions
                 WHERE input LIKE ?1 OR output LIKE ?1
                 ORDER BY pinned DESC, created_at DESC
                 LIMIT ?2 OFFSET ?3"
            )?;
            stmt.query_map(rusqlite::params![like_pattern, limit, offset], map_row)?.collect()
        }
    } else {
        let fts_query = format!("\"{}\"", query.replace('"', ""));
        if let Some(style) = style_filter {
            let mut stmt = conn.prepare(
                "SELECT c.id, c.input, c.output, c.style_id, c.intent, c.typo,
                        c.provider_id, c.model, c.pinned, c.created_at
                 FROM conversions c
                 JOIN conversions_fts f ON f.rowid = c.id
                 WHERE f.conversions_fts MATCH ?1 AND c.style_id = ?2
                 ORDER BY c.pinned DESC, c.created_at DESC
                 LIMIT ?3 OFFSET ?4"
            )?;
            stmt.query_map(rusqlite::params![fts_query, style, limit, offset], map_row)?.collect()
        } else {
            let mut stmt = conn.prepare(
                "SELECT c.id, c.input, c.output, c.style_id, c.intent, c.typo,
                        c.provider_id, c.model, c.pinned, c.created_at
                 FROM conversions c
                 JOIN conversions_fts f ON f.rowid = c.id
                 WHERE f.conversions_fts MATCH ?1
                 ORDER BY c.pinned DESC, c.created_at DESC
                 LIMIT ?2 OFFSET ?3"
            )?;
            stmt.query_map(rusqlite::params![fts_query, limit, offset], map_row)?.collect()
        }
    }
}
```

`pin_conversion` 関数:
```rust
pub fn pin_conversion(conn: &Connection, id: i64, pinned: bool) -> Result<(), rusqlite::Error> {
    conn.execute(
        "UPDATE conversions SET pinned = ?1 WHERE id = ?2",
        rusqlite::params![pinned as i64, id],
    )?;
    Ok(())
}
```

`delete_conversion` 関数:
```rust
pub fn delete_conversion(conn: &Connection, id: i64) -> Result<(), rusqlite::Error> {
    conn.execute("DELETE FROM conversions WHERE id = ?1", rusqlite::params![id])?;
    Ok(())
}
```

`enforce_history_limit` 関数:
```rust
pub fn enforce_history_limit(conn: &Connection, limit: i64) -> Result<u64, rusqlite::Error> {
    let count: i64 = conn.query_row("SELECT COUNT(*) FROM conversions", [], |r| r.get(0))?;
    if count > limit {
        let excess = count - limit;
        let deleted = conn.execute(
            "DELETE FROM conversions WHERE id IN (
                SELECT id FROM conversions WHERE pinned = 0 ORDER BY created_at ASC LIMIT ?1
            )",
            rusqlite::params![excess],
        )?;
        Ok(deleted as u64)
    } else {
        Ok(0)
    }
}
```

**2. commands/history.rs に新コマンド追加:**

既存 `get_history` の下に:
```rust
#[tauri::command]
pub async fn search_history(
    state: tauri::State<'_, crate::state::AppState>,
    query: String,
    style_filter: Option<String>,
    limit: i64,
    offset: i64,
) -> Result<Vec<ConversionRecord>, String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::conversions::search_history(&conn, &query, style_filter.as_deref(), limit, offset)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn pin_history(
    state: tauri::State<'_, crate::state::AppState>,
    id: i64,
    pinned: bool,
) -> Result<(), String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::conversions::pin_conversion(&conn, id, pinned)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn delete_history(
    state: tauri::State<'_, crate::state::AppState>,
    id: i64,
) -> Result<(), String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::conversions::delete_conversion(&conn, id)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn set_history_limit(
    state: tauri::State<'_, crate::state::AppState>,
    limit: i64,
) -> Result<(), String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::settings::set_setting(&conn, "history_limit", &limit.to_string())?;
        crate::db::conversions::enforce_history_limit(&conn, limit)?;
        Ok::<(), rusqlite::Error>(())
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn get_history_limit(
    state: tauri::State<'_, crate::state::AppState>,
) -> Result<i64, String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        match crate::db::settings::get_setting(&conn, "history_limit")? {
            Some(v) => v.parse::<i64>().map_err(|e| rusqlite::Error::ToSqlConversionFailure(Box::new(e))),
            None => Ok(1000), // default
        }
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/db/conversions.rs contains `pub fn search_history(`
    - src-tauri/src/db/conversions.rs contains `pub fn pin_conversion(`
    - src-tauri/src/db/conversions.rs contains `pub fn delete_conversion(`
    - src-tauri/src/db/conversions.rs contains `pub fn enforce_history_limit(`
    - src-tauri/src/db/conversions.rs contains `pinned DESC, created_at DESC` in get_history
    - src-tauri/src/db/conversions.rs contains `chars().count() < 3` for LIKE fallback
    - src-tauri/src/commands/history.rs contains `pub async fn search_history(`
    - src-tauri/src/commands/history.rs contains `pub async fn pin_history(`
    - src-tauri/src/commands/history.rs contains `pub async fn delete_history(`
    - src-tauri/src/commands/history.rs contains `pub async fn set_history_limit(`
    - src-tauri/src/commands/history.rs contains `pub async fn get_history_limit(`
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
  <done>履歴の検索・ピン・削除・件数制限の DB 関数とコマンドが実装済み、pinned DESC ソート対応</done>
</task>

<task type="auto">
  <name>Task 3: プロバイダー管理 + カスタムスタイル CRUD + 全コマンド登録</name>
  <files>
    src-tauri/src/db/custom_styles.rs,
    src-tauri/src/db/mod.rs,
    src-tauri/src/commands/styles.rs,
    src-tauri/src/commands/providers.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/providers/mod.rs,
    src-tauri/src/lib.rs
  </files>
  <read_first>
    src-tauri/src/db/custom_styles.rs,
    src-tauri/src/db/mod.rs,
    src-tauri/src/commands/providers.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/providers/mod.rs,
    src-tauri/src/lib.rs,
    src-tauri/src/keychain.rs,
    src-tauri/src/state.rs,
    providers.json
  </read_first>
  <action>
**1. 新規ファイル: src-tauri/src/db/custom_styles.rs**

```rust
use rusqlite::Connection;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CustomStyle {
    pub id: String,
    pub label: String,
    pub emoji: String,
    pub prompt: String,
    pub sort_order: i64,
    pub created_at: String,
}

pub fn get_custom_styles(conn: &Connection) -> Result<Vec<CustomStyle>, rusqlite::Error> {
    let mut stmt = conn.prepare(
        "SELECT id, label, emoji, prompt, sort_order, created_at FROM custom_styles ORDER BY sort_order ASC, created_at ASC"
    )?;
    let rows = stmt.query_map([], |row| {
        Ok(CustomStyle {
            id: row.get(0)?,
            label: row.get(1)?,
            emoji: row.get(2)?,
            prompt: row.get(3)?,
            sort_order: row.get(4)?,
            created_at: row.get(5)?,
        })
    })?;
    rows.collect()
}

pub fn insert_custom_style(
    conn: &Connection,
    id: &str, label: &str, emoji: &str, prompt: &str, sort_order: i64,
) -> Result<(), rusqlite::Error> {
    conn.execute(
        "INSERT INTO custom_styles (id, label, emoji, prompt, sort_order) VALUES (?1, ?2, ?3, ?4, ?5)",
        rusqlite::params![id, label, emoji, prompt, sort_order],
    )?;
    Ok(())
}

pub fn update_custom_style(
    conn: &Connection,
    id: &str, label: &str, emoji: &str, prompt: &str,
) -> Result<(), rusqlite::Error> {
    conn.execute(
        "UPDATE custom_styles SET label = ?2, emoji = ?3, prompt = ?4 WHERE id = ?1",
        rusqlite::params![id, label, emoji, prompt],
    )?;
    Ok(())
}

pub fn delete_custom_style(conn: &Connection, id: &str) -> Result<(), rusqlite::Error> {
    conn.execute("DELETE FROM custom_styles WHERE id = ?1", rusqlite::params![id])?;
    Ok(())
}
```

**2. db/mod.rs に追加:**
`pub mod custom_styles;` を `pub mod settings;` の下に追加。

**3. 新規ファイル: src-tauri/src/commands/styles.rs**

```rust
use crate::db::custom_styles::CustomStyle;

#[tauri::command]
pub async fn list_styles(
    state: tauri::State<'_, crate::state::AppState>,
) -> Result<Vec<CustomStyle>, String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::custom_styles::get_custom_styles(&conn)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn create_style(
    state: tauri::State<'_, crate::state::AppState>,
    id: String, label: String, emoji: String, prompt: String, sort_order: i64,
) -> Result<(), String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::custom_styles::insert_custom_style(&conn, &id, &label, &emoji, &prompt, sort_order)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn update_style(
    state: tauri::State<'_, crate::state::AppState>,
    id: String, label: String, emoji: String, prompt: String,
) -> Result<(), String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::custom_styles::update_custom_style(&conn, &id, &label, &emoji, &prompt)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn delete_style(
    state: tauri::State<'_, crate::state::AppState>,
    id: String,
) -> Result<(), String> {
    let db = state.db.clone();
    tokio::task::spawn_blocking(move || {
        let conn = db.blocking_lock();
        crate::db::custom_styles::delete_custom_style(&conn, &id)
    })
    .await.map_err(|e| e.to_string())?.map_err(|e| e.to_string())
}
```

**4. commands/providers.rs を拡張 — 既存の `list_providers` はそのまま残し、以下を追加:**

```rust
use crate::providers::{ProviderConfig, ProvidersFile};

#[tauri::command]
pub async fn get_provider_config(
    state: tauri::State<'_, crate::state::AppState>,
) -> Result<ProvidersFile, String> {
    let app_data_dir = state.app_data_dir.clone();
    tokio::task::spawn_blocking(move || {
        crate::providers::load_providers_config(&app_data_dir)
    })
    .await.map_err(|e| e.to_string())?
}

#[tauri::command]
pub async fn upsert_provider(
    state: tauri::State<'_, crate::state::AppState>,
    config: ProviderConfig,
) -> Result<(), String> {
    let app_data_dir = state.app_data_dir.clone();
    // 1. API キーが実値なら Keychain に保存
    if let Some(ref key) = config.api_key {
        if key != "<encrypted>" && !key.is_empty() && key != "ollama" {
            crate::keychain::set_api_key(&state.keychain_lock, &config.id, key)?;
        }
    }
    // 2. providers.json を更新 (api_key は "<encrypted>" に置換)
    tokio::task::spawn_blocking(move || {
        let mut pf = crate::providers::load_providers_config(&app_data_dir)?;
        let mut save_config = config.clone();
        // "<encrypted>" に正規化 (ollama 等を除く)
        if save_config.api_key.as_deref() != Some("ollama") && save_config.api_key.as_deref() != Some("") {
            save_config.api_key = Some("<encrypted>".to_string());
        }
        if let Some(existing) = pf.providers.iter_mut().find(|p| p.id == save_config.id) {
            *existing = save_config;
        } else {
            pf.providers.push(save_config);
        }
        let json = serde_json::to_string_pretty(&pf).map_err(|e| e.to_string())?;
        let path = app_data_dir.join("providers.json");
        std::fs::write(&path, json).map_err(|e| e.to_string())?;
        Ok::<(), String>(())
    })
    .await.map_err(|e| e.to_string())?
}

#[tauri::command]
pub async fn delete_provider(
    state: tauri::State<'_, crate::state::AppState>,
    provider_id: String,
) -> Result<(), String> {
    let app_data_dir = state.app_data_dir.clone();
    // Keychain エントリ削除 (エラーは無視 — エントリがない場合もある)
    let _ = crate::keychain::delete_api_key(&state.keychain_lock, &provider_id);
    tokio::task::spawn_blocking(move || {
        let mut pf = crate::providers::load_providers_config(&app_data_dir)?;
        pf.providers.retain(|p| p.id != provider_id);
        let json = serde_json::to_string_pretty(&pf).map_err(|e| e.to_string())?;
        std::fs::write(app_data_dir.join("providers.json"), json).map_err(|e| e.to_string())?;
        Ok::<(), String>(())
    })
    .await.map_err(|e| e.to_string())?
}

#[tauri::command]
pub async fn ping_provider(
    base_url: String,
    api_key: Option<String>,
) -> Result<String, String> {
    let url = format!("{}/models", base_url.trim_end_matches('/'));
    let client = reqwest::Client::builder()
        .timeout(std::time::Duration::from_secs(5))
        .build()
        .map_err(|e| e.to_string())?;
    let mut req = client.get(&url);
    if let Some(ref key) = api_key {
        if !key.is_empty() && key != "ollama" {
            req = req.bearer_auth(key);
        }
    }
    match req.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            if status == 200 || status == 401 {
                Ok("ok".to_string())
            } else {
                Err(format!("HTTP {}", status))
            }
        }
        Err(e) => Err(e.to_string()),
    }
}

#[tauri::command]
pub async fn set_active_provider(
    state: tauri::State<'_, crate::state::AppState>,
    provider_id: String,
) -> Result<(), String> {
    let app_data_dir = state.app_data_dir.clone();
    tokio::task::spawn_blocking(move || {
        let mut pf = crate::providers::load_providers_config(&app_data_dir)?;
        pf.default_provider = provider_id;
        let json = serde_json::to_string_pretty(&pf).map_err(|e| e.to_string())?;
        std::fs::write(app_data_dir.join("providers.json"), json).map_err(|e| e.to_string())?;
        Ok::<(), String>(())
    })
    .await.map_err(|e| e.to_string())?
}
```

**5. AppState に `app_data_dir` フィールド追加:**

`src-tauri/src/state.rs` に `pub app_data_dir: std::path::PathBuf,` を追加。

`lib.rs` の setup() で AppState 構築時に `app_data_dir: app_data_dir.clone(),` を追加。

**6. keychain.rs に `delete_api_key` 関数追加:**

```rust
pub fn delete_api_key(
    keychain_lock: &Mutex<()>,
    provider_id: &str,
) -> Result<(), String> {
    let _guard = keychain_lock.lock().map_err(|e| format!("Lock error: {}", e))?;
    let entry = Entry::new("romaji-memo", provider_id)
        .map_err(|e| format!("Keychain entry error: {}", e))?;
    match entry.delete_credential() {
        Ok(()) => Ok(()),
        Err(keyring::Error::NoEntry) => Ok(()), // already gone
        Err(e) => Err(format!("Keychain delete error: {}", e)),
    }
}
```

**7. commands/mod.rs を更新:**
```rust
pub mod convert;
pub mod history;
pub mod providers;
pub mod styles;
```

**8. lib.rs の `invoke_handler` に全新コマンドを追加:**

`tauri::generate_handler![]` に追加:
```rust
commands::history::search_history,
commands::history::pin_history,
commands::history::delete_history,
commands::history::set_history_limit,
commands::history::get_history_limit,
commands::styles::list_styles,
commands::styles::create_style,
commands::styles::update_style,
commands::styles::delete_style,
commands::providers::get_provider_config,
commands::providers::upsert_provider,
commands::providers::delete_provider,
commands::providers::ping_provider,
commands::providers::set_active_provider,
```
  </action>
  <verify>
    <automated>cd /Users/ohya/workspaces/romaji-editor && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/db/custom_styles.rs exists and contains `pub fn get_custom_styles(`
    - src-tauri/src/db/custom_styles.rs contains `pub fn insert_custom_style(`
    - src-tauri/src/db/custom_styles.rs contains `pub fn update_custom_style(`
    - src-tauri/src/db/custom_styles.rs contains `pub fn delete_custom_style(`
    - src-tauri/src/db/mod.rs contains `pub mod custom_styles`
    - src-tauri/src/commands/styles.rs exists and contains `pub async fn list_styles(`
    - src-tauri/src/commands/styles.rs contains `pub async fn create_style(`
    - src-tauri/src/commands/mod.rs contains `pub mod styles`
    - src-tauri/src/commands/providers.rs contains `pub async fn upsert_provider(`
    - src-tauri/src/commands/providers.rs contains `pub async fn delete_provider(`
    - src-tauri/src/commands/providers.rs contains `pub async fn ping_provider(`
    - src-tauri/src/commands/providers.rs contains `pub async fn set_active_provider(`
    - src-tauri/src/commands/providers.rs contains `pub async fn get_provider_config(`
    - src-tauri/src/state.rs contains `app_data_dir`
    - src-tauri/src/keychain.rs contains `pub fn delete_api_key(`
    - src-tauri/src/lib.rs contains `commands::styles::list_styles`
    - src-tauri/src/lib.rs contains `commands::providers::upsert_provider`
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
  <done>プロバイダー管理(upsert/delete/ping/config/active)、カスタムスタイルCRUD、全コマンドが invoke_handler に登録済み、cargo build 成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend IPC -> Rust commands | すべての IPC 引数はユーザー入力を含む可能性がある |
| Rust -> OS Keychain | API キーの読み書き |
| Rust -> providers.json file | プロバイダー設定のファイル I/O |
| Rust -> SQLite | SQL クエリへのユーザー入力 |
| Rust -> external HTTP (ping) | 外部 API への接続テスト |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | search_history FTS5 MATCH | mitigate | FTS5 クエリ内のダブルクォートを除去 (`query.replace('"', "")`) + パラメータバインディングで SQL injection を防止 |
| T-02-02 | Information Disclosure | get_provider_config | mitigate | API キーは `"<encrypted>"` のまま返す。Keychain から実値を取得して返さない |
| T-02-03 | Tampering | upsert_provider api_key | mitigate | 実値の API キーは Keychain に即保存し、providers.json には `"<encrypted>"` のみ書き込む |
| T-02-04 | Spoofing | ping_provider base_url | accept | ユーザーが入力した URL に GET を送るだけ。5 秒タイムアウトで DoS を防止。ローカルアプリなのでリスク低 |
| T-02-05 | Denial of Service | enforce_history_limit | mitigate | pinned=0 のレコードのみ古い順に削除。ピン留めは保護される |
</threat_model>

<verification>
- `cargo build --manifest-path src-tauri/Cargo.toml` が成功する
- 全新コマンドが `invoke_handler` に登録されている
- FTS5 trigram migration が定義されている
- グローバルショートカットが Rust setup() で登録されている
</verification>

<success_criteria>
- cargo build 成功
- 15 個の新規 Tauri コマンドが登録済み
- MIGRATION_002 (FTS5 trigram) が定義済み
- グローバルショートカット (Cmd+Shift+R) がプラグインとして登録済み
- npm 依存 (@dnd-kit/*, @tauri-apps/plugin-global-shortcut) がインストール済み
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-driver-polish/02-01-SUMMARY.md`
</output>
